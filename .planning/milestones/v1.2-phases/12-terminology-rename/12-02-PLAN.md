---
phase: 12-terminology-rename
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/app/(dashboard)/property/_components/pm-kpis.tsx
  - src/app/(dashboard)/property/_components/pm-turn-list.tsx
  - src/app/(dashboard)/property/_components/mobile-turn-card.tsx
  - src/app/(dashboard)/property/turn/[id]/_components/turn-detail-view.tsx
  - src/app/(dashboard)/executive/_components/executive-kpis.tsx
  - src/app/(dashboard)/executive/_components/executive-kpi-skeleton.tsx
  - src/app/(dashboard)/executive/_components/health-gauge.tsx
  - src/app/components/page.tsx
  - src/lib/kpis/pm-kpis.test.ts
  - src/lib/kpis/executive-kpis.test.ts
  - src/lib/airtable/__tests__/mappers.test.ts
  - src/lib/kpis/health-score.test.ts
  - src/app/(dashboard)/executive/_components/executive-charts.test.tsx
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03, TERM-04]

must_haves:
  truths:
    - "No page in the application displays Make Ready — all turn-level labels say Turn"
    - "No unit status label reads Vacant — units show Off Market wherever status is surfaced"
    - "A grep of src/ for Make Ready, makeReady, and MakeReady returns zero results"
    - "A grep of src/ for Vacant as a unit status label returns zero results (route strings and file names are expected and unchanged)"
    - "TypeScript compiles with zero errors"
    - "All tests pass"
  artifacts:
    - path: "src/app/(dashboard)/property/_components/pm-kpis.tsx"
      provides: "PM KPI labels using Turn vocabulary"
      contains: "Active Turns"
    - path: "src/app/(dashboard)/executive/_components/executive-kpis.tsx"
      provides: "Executive KPI labels using Turn vocabulary"
      contains: "Turn Overview"
    - path: "src/app/(dashboard)/property/_components/mobile-turn-card.tsx"
      provides: "Mobile card with Off Market Date label"
      contains: "Off Market Date"
    - path: "src/lib/kpis/pm-kpis.test.ts"
      provides: "PM KPI tests using new property names"
      contains: "activeTurns"
  key_links:
    - from: "src/app/(dashboard)/property/_components/pm-kpis.tsx"
      to: "src/lib/kpis/pm-kpis.ts"
      via: "reads kpis.activeTurns and kpis.avgTurnTime"
      pattern: "kpis\\.activeTurns|kpis\\.avgTurnTime"
    - from: "src/app/(dashboard)/executive/_components/executive-kpis.tsx"
      to: "src/lib/kpis/executive-kpis.ts"
      via: "reads kpis.activeTurnsOpen"
      pattern: "kpis\\.activeTurnsOpen"
    - from: "src/app/(dashboard)/property/_components/mobile-turn-card.tsx"
      to: "src/lib/types/airtable.ts"
      via: "reads turn.offMarketDate"
      pattern: "turn\\.offMarketDate"
---

<objective>
Update all UI components and tests to use the new Turn/Job/Off Market vocabulary established by Plan 01's type changes.

Purpose: Resolves all TypeScript compile errors from Plan 01's type renames, updates all user-facing labels, and updates test assertions to match the new vocabulary.

Output: Full codebase consistency — zero "Make Ready" or "Vacant" (as status label) anywhere in src/, all tests passing, TypeScript compiling cleanly.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-terminology-rename/12-CONTEXT.md
@.planning/phases/12-terminology-rename/12-RESEARCH.md
@.planning/phases/12-terminology-rename/12-UI-SPEC.md
@.planning/phases/12-terminology-rename/12-01-SUMMARY.md

<interfaces>
<!-- Interfaces after Plan 01 renames (what consumers must use) -->

From src/lib/types/airtable.ts (after Plan 01):
```typescript
export interface TurnRequest {
  // ... other fields unchanged ...
  offMarketDate: string | null           // was vacantDate
  daysOffMarketUntilReady: number | null  // was daysVacantUntilReady
  // ... other fields unchanged ...
}
```

From src/lib/kpis/pm-kpis.ts (after Plan 01):
```typescript
export interface PMKPIResult {
  activeTurns: number                    // was activeMakeReadys
  completedLast30d: number
  completedLast7d: number
  avgTurnTime: number | null             // was avgMakeReadyTime
  projectedSpendMTD: number
  pastTargetCount: number
}
```

From src/lib/kpis/executive-kpis.ts (after Plan 01):
```typescript
export interface ExecutiveKPIResult {
  activeJobsOpen: number
  jobsTrendingPastTarget: number
  jobsCompleted30d: number
  backlogDelta: number
  avgTimeToComplete: number | null
  projectedCostExposure: number
  activeTurnsOpen: number                // was activeMakeReadysOpen
  pastTargetAlerts: { propertyName: string; unitNumber: string }[]
  trendingAlerts: { propertyName: string; unitNumber: string }[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update UI component labels and property references</name>
  <files>
    src/app/(dashboard)/property/_components/pm-kpis.tsx,
    src/app/(dashboard)/property/_components/pm-turn-list.tsx,
    src/app/(dashboard)/property/_components/mobile-turn-card.tsx,
    src/app/(dashboard)/property/turn/[id]/_components/turn-detail-view.tsx,
    src/app/(dashboard)/executive/_components/executive-kpis.tsx,
    src/app/(dashboard)/executive/_components/executive-kpi-skeleton.tsx,
    src/app/(dashboard)/executive/_components/health-gauge.tsx,
    src/app/components/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/property/_components/pm-kpis.tsx
    - src/app/(dashboard)/property/_components/pm-turn-list.tsx
    - src/app/(dashboard)/property/_components/mobile-turn-card.tsx
    - src/app/(dashboard)/property/turn/[id]/_components/turn-detail-view.tsx
    - src/app/(dashboard)/executive/_components/executive-kpis.tsx
    - src/app/(dashboard)/executive/_components/executive-kpi-skeleton.tsx
    - src/app/(dashboard)/executive/_components/health-gauge.tsx
    - src/app/components/page.tsx
    - .planning/phases/12-terminology-rename/12-RESEARCH.md
    - .planning/phases/12-terminology-rename/12-UI-SPEC.md
  </read_first>
  <action>
    Apply every label and property rename listed below. Each file section has the EXACT before/after strings.

    **`src/app/(dashboard)/executive/_components/executive-kpis.tsx`:**
    - Section header: `"Make Ready Overview"` becomes `"Turn Overview"`
    - KPI label: `label="Active Make Readys Open"` becomes `label="Active Turns Open"`
    - KPI value: `value={kpis.activeMakeReadysOpen}` becomes `value={kpis.activeTurnsOpen}`
    - KPI label: `label="Make Readys Past Target Time"` becomes `label="Turns Past Target Time"`
    - KPI label: `label="Make Readys Trending Past Target Date"` becomes `label="Turns Trending Past Target Date"`
    - If any aria-label or title attribute references old terminology, update to match new label

    **`src/app/(dashboard)/executive/_components/executive-kpi-skeleton.tsx`:**
    - Comment: `Make Ready Overview section skeleton` becomes `Turn Overview section skeleton`

    **`src/app/(dashboard)/property/_components/pm-kpis.tsx`:**
    - KPI value references: `kpis.avgMakeReadyTime` becomes `kpis.avgTurnTime` (2 occurrences)
    - KPI label: `label="Active Make Readys"` becomes `label="Active Turns"`
    - KPI value: `value={kpis.activeMakeReadys}` becomes `value={kpis.activeTurns}`
    - KPI label: `label="Avg Make Ready Time"` becomes `label="Avg Turn Time"`

    **`src/app/(dashboard)/property/_components/pm-turn-list.tsx`:**
    - Group header: `title="Make Readys Past Target Time"` becomes `title="Turns Past Target Time"`
    - Group header: `title="Active Make Readys (On Schedule)"` becomes `title="Active Turns (On Schedule)"`

    **`src/app/(dashboard)/property/_components/mobile-turn-card.tsx`:**
    - Field label: `<span>Vacant Date</span>` becomes `<span>Off Market Date</span>`
    - Data binding: `{formatDate(turn.vacantDate)}` becomes `{formatDate(turn.offMarketDate)}`

    **`src/app/(dashboard)/property/turn/[id]/_components/turn-detail-view.tsx`:**
    - Field label: `Vacant Date` becomes `Off Market Date`
    - Data binding: `{formatDate(turn.vacantDate)` becomes `{formatDate(turn.offMarketDate)`
    - Field label: `Days Vacant` becomes `Days Off Market`
    - Data binding: `{turn.daysVacantUntilReady` becomes `{turn.daysOffMarketUntilReady`

    **`src/app/(dashboard)/executive/_components/health-gauge.tsx`:**
    - Description: `"% of turns completed within 10 days vacant"` becomes `"% of turns completed within 10 days off market"`

    **`src/app/components/page.tsx`:**
    - Component showcase label: `label="Active Make Readys"` becomes `label="Active Turns"`

    **LOCKED CONSTRAINTS (do NOT change):**
    - Do NOT rename any file names
    - Do NOT change `/vacant` route strings
    - Do NOT change `href="/vacant"` links
    - Do NOT change Airtable field strings in square brackets
    - Sidebar and bottom-tab-bar already read "Add Off Market" — verify but do NOT edit
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `executive-kpis.tsx` contains `"Turn Overview"` (not `"Make Ready Overview"`)
    - `executive-kpis.tsx` contains `label="Active Turns Open"` (not `"Active Make Readys Open"`)
    - `executive-kpis.tsx` contains `value={kpis.activeTurnsOpen}` (not `activeMakeReadysOpen`)
    - `executive-kpis.tsx` contains `label="Turns Past Target Time"` and `label="Turns Trending Past Target Date"`
    - `pm-kpis.tsx` contains `label="Active Turns"` and `label="Avg Turn Time"`
    - `pm-kpis.tsx` contains `kpis.activeTurns` and `kpis.avgTurnTime`
    - `pm-turn-list.tsx` contains `title="Turns Past Target Time"` and `title="Active Turns (On Schedule)"`
    - `mobile-turn-card.tsx` contains `Off Market Date` and `turn.offMarketDate`
    - `turn-detail-view.tsx` contains `Off Market Date`, `Days Off Market`, `turn.offMarketDate`, `turn.daysOffMarketUntilReady`
    - `health-gauge.tsx` contains `"10 days off market"` (not `"10 days vacant"`)
    - `page.tsx` (components) contains `label="Active Turns"`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All UI labels display Turn/Off Market vocabulary. No "Make Ready" or "Vacant" (as status) visible in any component. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update all test files to match renamed properties</name>
  <files>
    src/lib/kpis/pm-kpis.test.ts,
    src/lib/kpis/executive-kpis.test.ts,
    src/lib/airtable/__tests__/mappers.test.ts,
    src/lib/kpis/health-score.test.ts,
    src/app/(dashboard)/executive/_components/executive-charts.test.tsx
  </files>
  <read_first>
    - src/lib/kpis/pm-kpis.test.ts
    - src/lib/kpis/executive-kpis.test.ts
    - src/lib/airtable/__tests__/mappers.test.ts
    - src/lib/kpis/health-score.test.ts
    - src/app/(dashboard)/executive/_components/executive-charts.test.tsx
    - .planning/phases/12-terminology-rename/12-RESEARCH.md
  </read_first>
  <action>
    Update all test files to use the new property names and labels. Apply these renames in each file:

    **`src/lib/kpis/pm-kpis.test.ts`:**
    - All `activeMakeReadys` references become `activeTurns` (in expectations, describe blocks, variable names)
    - All `avgMakeReadyTime` references become `avgTurnTime`
    - All `daysVacantUntilReady` references become `daysOffMarketUntilReady` (in test fixture objects)
    - All `vacantDate` references become `offMarketDate` (in test fixture objects)
    - Describe block text: update any "Make Ready" to "Turn" and "Vacant" to "Off Market" in test descriptions
    - Factory function `makeTurnRequest` (or similar): update property names in default object to `offMarketDate` and `daysOffMarketUntilReady`

    **`src/lib/kpis/executive-kpis.test.ts`:**
    - All `activeMakeReadysOpen` references become `activeTurnsOpen`
    - All `daysVacantUntilReady` references become `daysOffMarketUntilReady` (in test fixture objects)
    - All `vacantDate` references become `offMarketDate` (in test fixture objects)
    - Describe block text: update any "Make Ready" to "Turn" in test descriptions

    **`src/lib/airtable/__tests__/mappers.test.ts`:**
    - Property assertions: `expect(result.vacantDate)` becomes `expect(result.offMarketDate)`
    - Property assertions: `expect(result.daysVacantUntilReady)` becomes `expect(result.daysOffMarketUntilReady)`
    - Do NOT change Airtable field strings in test fixtures (e.g., `'Vacant Date': '2024-01-15'` stays as-is — these simulate Airtable API responses)
    - Test description text about routes/URLs referencing `/vacant` stays as-is

    **`src/lib/kpis/health-score.test.ts`:**
    - All `daysVacantUntilReady` references become `daysOffMarketUntilReady`
    - All `vacantDate` references become `offMarketDate`
    - Update describe block text if it references "Vacant"

    **`src/app/(dashboard)/executive/_components/executive-charts.test.tsx`:**
    - All `activeMakeReadysOpen` references become `activeTurnsOpen`
    - All `daysVacantUntilReady` references become `daysOffMarketUntilReady`
    - All `vacantDate` references become `offMarketDate`

    **LOCKED CONSTRAINTS:**
    - Test descriptions that reference `/vacant` as a route path stay as-is (the route is unchanged)
    - Airtable field strings in test fixture data (`'Vacant Date'`, `'Days Vacant Until Ready'`) stay as-is — they simulate Airtable API responses
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && npm test -- --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pm-kpis.test.ts` contains `activeTurns` and `avgTurnTime` (not `activeMakeReadys` or `avgMakeReadyTime`)
    - `pm-kpis.test.ts` contains `offMarketDate` and `daysOffMarketUntilReady` (not `vacantDate` or `daysVacantUntilReady` as TS property names)
    - `executive-kpis.test.ts` contains `activeTurnsOpen` (not `activeMakeReadysOpen`)
    - `mappers.test.ts` contains `result.offMarketDate` and `result.daysOffMarketUntilReady` in assertions
    - `mappers.test.ts` still contains `'Vacant Date'` and `'Days Vacant Until Ready'` in Airtable fixture strings
    - `health-score.test.ts` contains `daysOffMarketUntilReady` and `offMarketDate`
    - `executive-charts.test.tsx` contains `activeTurnsOpen`
    - `npm test` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>All test files updated to match new vocabulary. Full test suite passes. No test references old property names.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification — zero legacy terminology in codebase</name>
  <files></files>
  <read_first>
    - src/components/layout/sidebar.tsx
    - src/components/layout/bottom-tab-bar.tsx
  </read_first>
  <action>
    Run the full verification suite to confirm Phase 12 is complete:

    1. **Verify nav labels (read-only check):**
       - Confirm sidebar.tsx contains "Add Off Market" (or similar "Off Market" label) — do NOT edit
       - Confirm bottom-tab-bar.tsx contains "Off Market" label — do NOT edit

    2. **Run Make Ready grep:**
       ```
       grep -r "Make Ready\|makeReady\|MakeReady" src/
       ```
       Must return 0 results.

    3. **Run Vacant grep (filtered):**
       ```
       grep -rn "Vacant\|vacant" src/ | grep -v "'/vacant'" | grep -v "add-vacant-form" | grep -v "vacant\.ts" | grep -v "node_modules" | grep -v "\.test\." | grep -v "f\['Vacant" | grep -v "f\['Days Vacant"
       ```
       Must return 0 results (route strings, file names, and Airtable field strings are expected).

       Also check test files separately for property-level "vacant" (excluding Airtable fixture strings):
       ```
       grep -rn "\.vacantDate\|\.daysVacantUntilReady\|vacantDate:" src/
       ```
       Must return 0 results (Airtable fixture field strings like `'Vacant Date': value` are fine).

    4. **TypeScript compilation:**
       ```
       npx tsc --noEmit
       ```
       Must exit with code 0.

    5. **Full test suite:**
       ```
       npm test
       ```
       Must exit with code 0.

    If ANY check fails, fix the remaining occurrence and re-run.
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && grep -rc "Make Ready\|makeReady\|MakeReady" src/ | grep -v ":0$" | wc -l && grep -rc "\.vacantDate\|\.daysVacantUntilReady\|activeMakeReadys\|avgMakeReadyTime\|activeMakeReadysOpen" src/ | grep -v ":0$" | wc -l && npx tsc --noEmit 2>&1 | tail -3 && npm test -- --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "Make Ready\|makeReady\|MakeReady" src/` returns 0 results
    - `grep -r "\.vacantDate\|\.daysVacantUntilReady" src/` returns 0 results
    - `grep -r "activeMakeReadys\|avgMakeReadyTime\|activeMakeReadysOpen" src/` returns 0 results
    - `npx tsc --noEmit` exits with code 0
    - `npm test` exits with code 0
    - sidebar.tsx contains "Off Market" in a nav label
    - bottom-tab-bar.tsx contains "Off Market" in a nav label
  </acceptance_criteria>
  <done>Phase 12 complete. Zero legacy terminology in source. TypeScript compiles. All tests pass. Nav labels verified.</done>
</task>

</tasks>

<verification>
Phase 12 is complete when ALL of these pass:
1. `grep -r "Make Ready\|makeReady\|MakeReady" src/` — 0 results
2. `grep -r "\.vacantDate\|\.daysVacantUntilReady" src/` — 0 results
3. `grep -r "activeMakeReadys\|avgMakeReadyTime\|activeMakeReadysOpen" src/` — 0 results
4. `npx tsc --noEmit` — exit code 0
5. `npm test` — exit code 0
6. Sidebar and bottom-tab-bar display "Off Market" (verified by read, not changed)
</verification>

<success_criteria>
- No page in the application displays "Make Ready" — all turn-level events labeled "Turn"
- No unit status label reads "Vacant" — units show "Off Market"
- A search of src/ for "Make Ready", "makeReady", and "Vacant" (as unit status) returns zero results
- TypeScript types, variable names use turn/job vocabulary with no legacy identifiers remaining
- All 202+ existing tests pass with updated assertions
</success_criteria>

<output>
After completion, create `.planning/phases/12-terminology-rename/12-02-SUMMARY.md`
</output>
