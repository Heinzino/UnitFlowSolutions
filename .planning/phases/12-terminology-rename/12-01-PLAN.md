---
phase: 12-terminology-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/airtable.ts
  - src/lib/kpis/pm-kpis.ts
  - src/lib/kpis/executive-kpis.ts
  - src/lib/airtable/tables/mappers.ts
autonomous: true
requirements: [TERM-01, TERM-02, TERM-04]

must_haves:
  truths:
    - "TurnRequest interface uses offMarketDate and daysOffMarketUntilReady instead of vacantDate and daysVacantUntilReady"
    - "PMKPIResult uses activeTurns and avgTurnTime instead of activeMakeReadys and avgMakeReadyTime"
    - "ExecutiveKPIResult uses activeTurnsOpen instead of activeMakeReadysOpen"
    - "Mapper assigns Airtable field values to new property names while keeping Airtable field strings unchanged"
  artifacts:
    - path: "src/lib/types/airtable.ts"
      provides: "TurnRequest with renamed properties"
      contains: "offMarketDate"
    - path: "src/lib/kpis/pm-kpis.ts"
      provides: "PMKPIResult with renamed fields"
      contains: "activeTurns"
    - path: "src/lib/kpis/executive-kpis.ts"
      provides: "ExecutiveKPIResult with renamed field"
      contains: "activeTurnsOpen"
    - path: "src/lib/airtable/tables/mappers.ts"
      provides: "Mapper with renamed output properties"
      contains: "offMarketDate"
  key_links:
    - from: "src/lib/airtable/tables/mappers.ts"
      to: "src/lib/types/airtable.ts"
      via: "mapTurnRequest returns TurnRequest"
      pattern: "offMarketDate.*f\\['Vacant Date'\\]"
    - from: "src/lib/kpis/pm-kpis.ts"
      to: "src/lib/types/airtable.ts"
      via: "computePMKPIs reads TurnRequest.daysOffMarketUntilReady"
      pattern: "tr\\.daysOffMarketUntilReady"
    - from: "src/lib/kpis/executive-kpis.ts"
      to: "src/lib/types/airtable.ts"
      via: "computeExecutiveKPIs reads TurnRequest.daysOffMarketUntilReady"
      pattern: "tr\\.daysOffMarketUntilReady"
---

<objective>
Rename all type definitions, KPI interfaces, and the Airtable mapper to use Turn/Job/Off Market vocabulary instead of Make Ready/Vacant.

Purpose: Establishes the new vocabulary at the type system level, creating TypeScript compile errors at every consumer that still uses old names. This makes Plan 02's work self-verifying via `tsc --noEmit`.

Output: Four updated source files with renamed properties and identifiers. All downstream consumers will have TypeScript errors until Plan 02 completes.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-terminology-rename/12-CONTEXT.md
@.planning/phases/12-terminology-rename/12-RESEARCH.md

<interfaces>
<!-- Current interfaces that will be modified in this plan -->

From src/lib/types/airtable.ts:
```typescript
export interface TurnRequest {
  // ... other fields ...
  vacantDate: string | null           // RENAME to offMarketDate
  daysVacantUntilReady: number | null  // RENAME to daysOffMarketUntilReady
  // ... other fields ...
}
```

From src/lib/kpis/pm-kpis.ts:
```typescript
export interface PMKPIResult {
  activeMakeReadys: number             // RENAME to activeTurns
  completedLast30d: number
  completedLast7d: number
  avgMakeReadyTime: number | null      // RENAME to avgTurnTime
  projectedSpendMTD: number
  pastTargetCount: number
}
```

From src/lib/kpis/executive-kpis.ts:
```typescript
export interface ExecutiveKPIResult {
  activeJobsOpen: number
  jobsTrendingPastTarget: number
  jobsCompleted30d: number
  backlogDelta: number
  avgTimeToComplete: number | null
  projectedCostExposure: number
  activeMakeReadysOpen: number         // RENAME to activeTurnsOpen
  pastTargetAlerts: { propertyName: string; unitNumber: string }[]
  trendingAlerts: { propertyName: string; unitNumber: string }[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename type definitions and KPI interfaces</name>
  <files>src/lib/types/airtable.ts, src/lib/kpis/pm-kpis.ts, src/lib/kpis/executive-kpis.ts</files>
  <read_first>
    - src/lib/types/airtable.ts
    - src/lib/kpis/pm-kpis.ts
    - src/lib/kpis/executive-kpis.ts
    - .planning/phases/12-terminology-rename/12-RESEARCH.md
  </read_first>
  <action>
    **In `src/lib/types/airtable.ts` (TurnRequest interface):**
    - Rename property `vacantDate: string | null` to `offMarketDate: string | null`
    - Rename property `daysVacantUntilReady: number | null` to `daysOffMarketUntilReady: number | null`
    - Do NOT rename the `TurnRequest` interface name (already correct vocabulary)
    - Do NOT rename the `Job` interface name (already correct vocabulary)
    - Do NOT change any other properties

    **In `src/lib/kpis/pm-kpis.ts` (PMKPIResult interface + computePMKPIs function):**
    - Interface: rename `activeMakeReadys: number` to `activeTurns: number`
    - Interface: rename `avgMakeReadyTime: number | null` to `avgTurnTime: number | null`
    - Update null comment: `// null when no Done turn requests` stays as-is
    - Local variable: rename `const activeMakeReadys = ...` to `const activeTurns = ...`
    - Local variable: rename `const avgMakeReadyTime = ...` to `const avgTurnTime = ...`
    - Comment: `PM-01: Active Make Readys` becomes `PM-01: Active Turns`
    - Comment: `PM-04: Avg Make Ready Time` becomes `PM-04: Avg Turn Time`
    - Consumer property: `tr.daysVacantUntilReady` becomes `tr.daysOffMarketUntilReady` (line ~83, pastTargetCount filter)
    - Return object: `activeMakeReadys,` becomes `activeTurns,` and `avgMakeReadyTime,` becomes `avgTurnTime,`

    **In `src/lib/kpis/executive-kpis.ts` (ExecutiveKPIResult interface + computeExecutiveKPIs function):**
    - Interface: rename `activeMakeReadysOpen: number` to `activeTurnsOpen: number`
    - Local variable: rename `const activeMakeReadysOpen = ...` to `const activeTurnsOpen = ...`
    - Comment: `EXEC-04: Active Make Readys Open` becomes `EXEC-04: Active Turns Open`
    - Consumer property (line ~104): `tr.daysVacantUntilReady` becomes `tr.daysOffMarketUntilReady` (pastTargetAlerts filter)
    - Consumer property (line ~108): `tr.daysVacantUntilReady` becomes `tr.daysOffMarketUntilReady` (trendingAlerts filter)
    - Comment: `EXEC-05: Alert arrays — filtered by daysVacantUntilReady threshold` becomes `EXEC-05: Alert arrays — filtered by daysOffMarketUntilReady threshold`
    - Return object: `activeMakeReadysOpen,` becomes `activeTurnsOpen,`

    **CRITICAL: Do NOT change any Airtable field strings (anything in square brackets like f['Vacant Date']).**
    **CRITICAL: Do NOT rename file names — only change content within files.**
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && grep -c "offMarketDate" src/lib/types/airtable.ts && grep -c "activeTurns" src/lib/kpis/pm-kpis.ts && grep -c "activeTurnsOpen" src/lib/kpis/executive-kpis.ts && grep -c "vacantDate\|daysVacantUntilReady\|activeMakeReadys\|avgMakeReadyTime\|activeMakeReadysOpen" src/lib/types/airtable.ts src/lib/kpis/pm-kpis.ts src/lib/kpis/executive-kpis.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/types/airtable.ts` contains `offMarketDate: string | null` and `daysOffMarketUntilReady: number | null`
    - `src/lib/types/airtable.ts` does NOT contain `vacantDate` or `daysVacantUntilReady`
    - `src/lib/kpis/pm-kpis.ts` contains `activeTurns: number` and `avgTurnTime: number | null` in PMKPIResult
    - `src/lib/kpis/pm-kpis.ts` does NOT contain `activeMakeReadys` or `avgMakeReadyTime`
    - `src/lib/kpis/pm-kpis.ts` contains `tr.daysOffMarketUntilReady` (not `tr.daysVacantUntilReady`)
    - `src/lib/kpis/executive-kpis.ts` contains `activeTurnsOpen: number` in ExecutiveKPIResult
    - `src/lib/kpis/executive-kpis.ts` does NOT contain `activeMakeReadysOpen`
    - `src/lib/kpis/executive-kpis.ts` contains `tr.daysOffMarketUntilReady` (two occurrences, not `tr.daysVacantUntilReady`)
  </acceptance_criteria>
  <done>TurnRequest, PMKPIResult, and ExecutiveKPIResult interfaces use new vocabulary. All local variables and comments updated to match.</done>
</task>

<task type="auto">
  <name>Task 2: Rename mapper output properties</name>
  <files>src/lib/airtable/tables/mappers.ts</files>
  <read_first>
    - src/lib/airtable/tables/mappers.ts
    - src/lib/types/airtable.ts
  </read_first>
  <action>
    **In `src/lib/airtable/tables/mappers.ts` (mapTurnRequest function return object):**

    Rename the two property assignments in the return object. The LEFT side (property name) changes. The RIGHT side (Airtable field string) stays UNCHANGED.

    Before:
    ```
    vacantDate: f['Vacant Date'] ? String(f['Vacant Date']) : null,
    ```
    After:
    ```
    offMarketDate: f['Vacant Date'] ? String(f['Vacant Date']) : null,
    ```

    Before:
    ```
    daysVacantUntilReady:
      f['Days Vacant Until Ready'] != null
        ? Number(f['Days Vacant Until Ready']) || null
        : null,
    ```
    After:
    ```
    daysOffMarketUntilReady:
      f['Days Vacant Until Ready'] != null
        ? Number(f['Days Vacant Until Ready']) || null
        : null,
    ```

    **CRITICAL: The Airtable field strings `'Vacant Date'` and `'Days Vacant Until Ready'` MUST remain unchanged.** These match the Airtable schema and cannot be renamed from the application side. Only the TypeScript property name on the LEFT side of the `:` changes.

    Do NOT change anything else in the file — mapJob, buildJobFilterFormula, and all other mapTurnRequest properties stay the same.
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && grep "offMarketDate" src/lib/airtable/tables/mappers.ts && grep "daysOffMarketUntilReady" src/lib/airtable/tables/mappers.ts && grep "'Vacant Date'" src/lib/airtable/tables/mappers.ts && grep "'Days Vacant Until Ready'" src/lib/airtable/tables/mappers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `mappers.ts` contains `offMarketDate: f['Vacant Date']` (property renamed, field string unchanged)
    - `mappers.ts` contains `daysOffMarketUntilReady:` followed by `f['Days Vacant Until Ready']` (property renamed, field string unchanged)
    - `mappers.ts` does NOT contain `vacantDate:` as a property assignment
    - `mappers.ts` does NOT contain `daysVacantUntilReady:` as a property assignment
    - `mappers.ts` still contains `f['Vacant Date']` and `f['Days Vacant Until Ready']` (Airtable strings preserved)
  </acceptance_criteria>
  <done>Mapper output properties renamed to match TurnRequest interface. Airtable boundary preserved — field strings unchanged.</done>
</task>

</tasks>

<verification>
After both tasks, run:
1. `grep -r "vacantDate\|daysVacantUntilReady\|activeMakeReadys\|avgMakeReadyTime\|activeMakeReadysOpen" src/lib/types/ src/lib/kpis/ src/lib/airtable/` — should return 0 results
2. `npx tsc --noEmit` — will show errors in consumer files (expected, fixed in Plan 02)
</verification>

<success_criteria>
- Zero occurrences of `vacantDate`, `daysVacantUntilReady`, `activeMakeReadys`, `avgMakeReadyTime`, or `activeMakeReadysOpen` in the four modified files
- Airtable field strings `'Vacant Date'` and `'Days Vacant Until Ready'` preserved in mappers.ts
- `offMarketDate`, `daysOffMarketUntilReady`, `activeTurns`, `avgTurnTime`, `activeTurnsOpen` present in their respective files
</success_criteria>

<output>
After completion, create `.planning/phases/12-terminology-rename/12-01-SUMMARY.md`
</output>
