---
phase: 05-property-manager-view
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(dashboard)/property/page.tsx
  - src/app/(dashboard)/property/_components/pm-dashboard.tsx
  - src/app/(dashboard)/property/_components/pm-kpis.tsx
  - src/app/(dashboard)/property/_components/pm-turn-list.tsx
autonomous: true
requirements:
  - PM-01
  - PM-02
  - PM-03
  - PM-04

must_haves:
  truths:
    - "PM sees 'Make Readys Past Target Time' section above 'Active Make Readys (On Schedule)'"
    - "Overdue section uses pink header bar and is hidden when count is 0"
    - "Turn list shows all PM-03 columns: Property Name (badge), Unit Number, Status (pill), Ready To Lease Date, Vacant Date, Jobs, Price"
    - "PM with multiple properties can filter via dropdown; KPIs and list both update"
    - "Dropdown hidden when PM has only one property"
    - "KPI cards display all 6 metrics in 3x2 grid"
    - "Past Target Time KPI uses pink alert-past variant when count > 0"
    - "Rows are clickable and navigate to /property/turn/[id]"
  artifacts:
    - path: "src/app/(dashboard)/property/page.tsx"
      provides: "Server page: auth + user data, renders PMDashboard"
      min_lines: 15
    - path: "src/app/(dashboard)/property/_components/pm-dashboard.tsx"
      provides: "Client container owning selectedProperty state"
      exports: ["PMDashboard"]
    - path: "src/app/(dashboard)/property/_components/pm-kpis.tsx"
      provides: "Async server component rendering 6 KPI cards"
      exports: ["PMKPIs"]
    - path: "src/app/(dashboard)/property/_components/pm-turn-list.tsx"
      provides: "Async server component with overdue/on-schedule partition"
      exports: ["PMTurnList"]
  key_links:
    - from: "src/app/(dashboard)/property/_components/pm-kpis.tsx"
      to: "src/lib/kpis/pm-kpis.ts"
      via: "calls computePMKPIs"
      pattern: "computePMKPIs"
    - from: "src/app/(dashboard)/property/_components/pm-turn-list.tsx"
      to: "src/lib/airtable/tables/turn-requests.ts"
      via: "calls fetchTurnRequestsForUser"
      pattern: "fetchTurnRequestsForUser"
    - from: "src/app/(dashboard)/property/_components/pm-dashboard.tsx"
      to: "src/components/layout/property-selector.tsx"
      via: "renders PropertySelector with All Properties option"
      pattern: "PropertySelector"
    - from: "src/app/(dashboard)/property/_components/pm-turn-list.tsx"
      to: "src/app/(dashboard)/property/_components/job-status-dropdown.tsx"
      via: "renders JobStatusDropdown per job in turn rows"
      pattern: "JobStatusDropdown"
---

<objective>
Build the PM dashboard page with KPI cards, property filter, and the two-section turn list (overdue-first).

Purpose: This is the core PM view -- the daily workflow page. PMs open it, scan KPIs, spot overdue turns, and can do quick triage from the list. Property filtering keeps everything in sync (KPIs + list reflect the same scope).

Output: Fully functional /property page replacing the Phase 2 placeholder.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-property-manager-view/05-CONTEXT.md
@.planning/phases/05-property-manager-view/05-RESEARCH.md
@.planning/phases/05-property-manager-view/05-01-SUMMARY.md

@src/app/(dashboard)/property/page.tsx
@src/app/(dashboard)/executive/page.tsx
@src/app/(dashboard)/executive/_components/executive-kpis.tsx
@src/lib/airtable/tables/turn-requests.ts
@src/components/layout/property-selector.tsx
@src/components/ui/table.tsx
@src/components/ui/kpi-card.tsx
@src/components/ui/status-badge.tsx
@src/components/ui/badge.tsx
@src/components/ui/card.tsx

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/kpis/pm-kpis.ts:
```typescript
export interface PMKPIResult {
  activeMakeReadys: number
  completedLast30d: number
  completedLast7d: number
  avgMakeReadyTime: number | null
  projectedSpendMTD: number
  pastTargetCount: number
}
export function computePMKPIs(turnRequests: TurnRequest[]): PMKPIResult
```

From src/app/(dashboard)/property/_components/job-status-dropdown.tsx:
```typescript
interface JobStatusDropdownProps {
  jobId: number
  turnRequestId: number
  currentStatus: JobStatus
}
export function JobStatusDropdown(props: JobStatusDropdownProps): JSX.Element
```

From src/app/(dashboard)/property/_components/pm-kpi-skeleton.tsx:
```typescript
export function PMKPISkeleton(): JSX.Element
```

From src/app/(dashboard)/property/_components/pm-turn-list-skeleton.tsx:
```typescript
export function PMTurnListSkeleton(): JSX.Element
```

From src/components/layout/property-selector.tsx:
```typescript
interface PropertySelectorProps {
  properties: string[]
  selectedProperty: string | null
  onSelect: (property: string) => void
}
export function PropertySelector(props: PropertySelectorProps): JSX.Element
```

From src/lib/airtable/tables/turn-requests.ts:
```typescript
export async function fetchTurnRequestsForUser(
  role: UserRole,
  assignedPropertyNames: string[]
): Promise<TurnRequest[]>
```

From src/lib/types/airtable.ts:
```typescript
export interface TurnRequest {
  requestId: number
  readyToLeaseDate: string | null
  vacantDate: string | null
  status: string
  jobIds: number[]
  jobs?: Job[]
  quotePrice: string | null
  totalCost: string | null
  propertyName: string
  unitNumber: string
  daysVacantUntilReady: number | null
  created: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PMDashboard client container and PM page</name>
  <files>src/app/(dashboard)/property/page.tsx, src/app/(dashboard)/property/_components/pm-dashboard.tsx</files>
  <action>
    1. **Replace src/app/(dashboard)/property/page.tsx** (currently Phase 2 placeholder):
    - Keep server component pattern: createClient() + getUser() + redirect if no user
    - Extract from user: property_ids (string[]), role (string), displayName (full_name or email)
    - Render PMDashboard client component passing assignedProperties, role, displayName as props
    - Follow executive page.tsx pattern: page is synchronous auth-only, data fetching happens in children

    2. **Create src/app/(dashboard)/property/_components/pm-dashboard.tsx**:
    - "use client" component
    - Props: assignedProperties (string[]), role (string), displayName (string)
    - State: selectedProperty (string, default "") where "" = "All Properties"
    - Compute effectiveProperties: if selectedProperty === "" use full assignedProperties array, else [selectedProperty]
    - Render page header with title "Property Manager Dashboard", subtitle with displayName, text-white/text-white/70 on dark green background (Phase 4 pattern)
    - Conditionally render PropertySelector when assignedProperties.length > 1 (per user decision: hidden when PM has only one property)
    - Pass ["All Properties", ...assignedProperties] as properties to PropertySelector
    - Map "All Properties" selection back to "" sentinel for state
    - Render Suspense with key={`kpis-${selectedProperty}`} wrapping PMKPIs, fallback={PMKPISkeleton}
    - PMKPIs receives assignedProperties={effectiveProperties}
    - Render Suspense with key={`turns-${selectedProperty}`} wrapping PMTurnList, fallback={PMTurnListSkeleton}
    - PMTurnList receives assignedProperties={effectiveProperties} and role={role}
    - CRITICAL: Key Suspense on selectedProperty to force remount on filter change (Research pitfall 2)
    - Use PropertySelector directly (NOT PropertySelectorWrapper) per Research pitfall 1
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PM page authenticates user and renders PMDashboard. Property selector shows "All Properties" default. Changing property updates both Suspense boundaries via key remount.</done>
</task>

<task type="auto">
  <name>Task 2: Build PMKPIs server component and PMTurnList with overdue partition</name>
  <files>src/app/(dashboard)/property/_components/pm-kpis.tsx, src/app/(dashboard)/property/_components/pm-turn-list.tsx</files>
  <action>
    1. **Create src/app/(dashboard)/property/_components/pm-kpis.tsx**:
    - Async server component (NOT "use client")
    - Props: assignedProperties (string[])
    - Fetch: const turnRequests = await fetchTurnRequestsForUser("pm", assignedProperties)
    - Compute: const kpis = computePMKPIs(turnRequests)
    - Render 6 KPICards in a 3x2 grid (grid grid-cols-1 sm:grid-cols-3 gap-4):
      Row 1: Active Make Readys (Home icon) | Completed 30d (CheckCircle) | Completed 7d (Activity)
      Row 2: Avg Make Ready Time (Clock) | Projected Spend MTD (DollarSign) | Past Target Time (AlertTriangle)
    - Format avgMakeReadyTime: Math.round() + " days" or "N/A" if null
    - Format projectedSpendMTD: Intl.NumberFormat USD with maximumFractionDigits: 0
    - Past Target Time uses variant={kpis.pastTargetCount > 0 ? "alert-past" : "default"} per user decision
    - Import icons from lucide-react
    - Follow ExecutiveKPIs pattern exactly

    2. **Create src/app/(dashboard)/property/_components/pm-turn-list.tsx**:
    - Async server component
    - Props: assignedProperties (string[]), role (string)
    - Fetch: const turnRequests = await fetchTurnRequestsForUser(role as UserRole, assignedProperties)
    - Partition active TRs (status !== "Done"):
      - overdue: daysVacantUntilReady > 10
      - onSchedule: daysVacantUntilReady <= 10 or null
    - Render overdue section FIRST (PM-01) -- hidden entirely when overdue.length === 0 (user decision)
    - Overdue section: Card with pink/alert-past header bar (bg-alert-past-target text), section title "Make Readys Past Target Time"
    - On-schedule section: Card with default header, title "Active Make Readys (On Schedule)" (PM-02)
    - Both sections use Table compound component with columns (PM-03):
      - Property Name: Badge component with property name text
      - Unit Number: plain text
      - Status: StatusBadge component with status string
      - Ready To Lease Date: formatted date or "---"
      - Vacant Date: formatted date or "---"
      - Jobs: count badge showing jobIds.length (e.g., "3 jobs")
      - Price: Intl.NumberFormat on totalCost (fallback quotePrice) or "---"
    - Rows are clickable: use Link from next/link wrapping the row, navigating to /property/turn/{requestId}
    - For each turn row's jobs, render a JobStatusDropdown per job in a status column ONLY if turn has resolved jobs. Since turns on the list have jobIds but not necessarily resolved jobs array, show the job count badge. The per-job status dropdown is primarily for the detail page. On the list, show turn status via StatusBadge (the turn's own status field).
    - IMPORTANT: Do NOT try to show per-job status dropdowns on the list rows. The turn list shows turn-level data. Job-level status updates happen on the detail page (PM-08 says "from turn detail").
    - On mobile: Consider a stacked card layout variant. At minimum, use responsive table with horizontal scroll on small screens.
    - Date formatting: use short format like "Mar 12, 2026"
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PMKPIs renders 6 KPI cards with correct data from fetchTurnRequestsForUser. PMTurnList partitions turns into overdue (pink header, shown first) and on-schedule sections with all PM-03 columns. Overdue section hidden when empty. Rows link to /property/turn/[id].</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no type errors
- `npx vitest run` -- all existing tests still pass
- Dev server: /property page loads with KPI grid and turn list sections
</verification>

<success_criteria>
- PM page shows 6 KPI cards in 3x2 grid with correct values
- Past Target Time KPI uses pink variant when count > 0
- Overdue turns section (pink header) appears above on-schedule section
- Overdue section hidden when no overdue turns exist
- Property dropdown appears only when PM has multiple properties
- Selecting a property updates both KPIs and turn list
- Turn rows show all PM-03 columns and link to /property/turn/[id]
</success_criteria>

<output>
After completion, create `.planning/phases/05-property-manager-view/05-02-SUMMARY.md`
</output>
