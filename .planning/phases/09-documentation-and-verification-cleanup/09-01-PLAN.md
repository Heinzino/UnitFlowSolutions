---
phase: 09-documentation-and-verification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-airtable-data-layer/03-VERIFICATION.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "03-VERIFICATION.md exists at .planning/phases/03-airtable-data-layer/03-VERIFICATION.md"
    - "All 6 DATA requirements (DATA-01 through DATA-06) have Observable Truth rows with VERIFIED status and specific code evidence"
    - "Commits Verified section lists all 6 commit SHAs from Phase 3 SUMMARY files"
    - "Required Artifacts section references actual source files and test files with correct paths"
    - "Human Verification section identifies live-environment-only checks (Airtable API calls, cache TTL behavior)"
  artifacts:
    - path: ".planning/phases/03-airtable-data-layer/03-VERIFICATION.md"
      provides: "Phase 3 verification report documenting all DATA requirement evidence"
      contains: "DATA-01"
  key_links:
    - from: "03-VERIFICATION.md Observable Truths"
      to: "03-01/02/03-SUMMARY.md evidence"
      via: "commit SHAs and file paths cited in evidence column"
      pattern: "VERIFIED.*src/lib/airtable"
---

<objective>
Write the missing Phase 3 VERIFICATION.md documenting all 6 DATA requirements against existing code artifacts.

Purpose: Phase 3 was fully implemented and tested but no formal VERIFICATION.md was written. The v1.0 milestone audit identified this gap. All evidence already exists in SUMMARY files and source code -- this task inspects artifacts and writes the verification document.

Output: `.planning/phases/03-airtable-data-layer/03-VERIFICATION.md`
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-and-verification-cleanup/09-RESEARCH.md

# Phase 3 evidence sources
@.planning/phases/03-airtable-data-layer/03-01-SUMMARY.md
@.planning/phases/03-airtable-data-layer/03-02-SUMMARY.md
@.planning/phases/03-airtable-data-layer/03-03-SUMMARY.md
@.planning/phases/03-airtable-data-layer/03-UAT.md

# Format reference
@.planning/phases/07-notifications-charts-and-vendor-metrics/07-VERIFICATION.md

# Source files to inspect for evidence
@src/lib/airtable/client.ts
@src/lib/airtable/rate-limiter.ts
@src/lib/airtable/tables/mappers.ts
@src/lib/airtable/tables/turn-requests.ts
@src/app/actions/job-status.ts
@next.config.ts
@src/lib/types/airtable.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect Phase 3 source files and test files for verification evidence</name>
  <files>
    src/lib/airtable/client.ts,
    src/lib/airtable/rate-limiter.ts,
    src/lib/airtable/tables/mappers.ts,
    src/lib/airtable/tables/turn-requests.ts,
    src/app/actions/job-status.ts,
    next.config.ts,
    src/lib/types/airtable.ts,
    src/lib/airtable/__tests__/client.test.ts,
    src/lib/airtable/__tests__/rate-limiter.test.ts,
    src/lib/airtable/__tests__/mappers.test.ts,
    src/app/actions/__tests__/job-status.test.ts
  </files>
  <action>
    Read each source file and test file listed above. For each DATA requirement, collect specific evidence:

    - DATA-01: In client.ts, confirm `process.env.AIRTABLE_API_KEY` (no NEXT_PUBLIC_ prefix). In client.test.ts, count tests and note their names.
    - DATA-02: In types/airtable.ts, list all 9 interface names. In mappers.test.ts, count tests (describe/it blocks).
    - DATA-03: In next.config.ts, confirm `cacheComponents: true` and the airtableData cache profile values (stale, revalidate, expire).
    - DATA-04: In rate-limiter.ts, confirm TokenBucket instantiation with capacity=5, refillRate=5/1000. In rate-limiter.test.ts, count tests and note their names.
    - DATA-05: In turn-requests.ts, find resolveLinkedJobs function and confirm single-call batch pattern. In mappers.ts, find buildJobFilterFormula. In mappers.test.ts, find tests for buildJobFilterFormula.
    - DATA-06: In job-status.ts, confirm revalidateTag calls with { expire: 0 } and count cache tags busted. In job-status.test.ts, count tests and note revalidateTag assertion.

    Also read 03-UAT.md to note the pre-existing issue (Next.js blocking route error, fixed in Phase 4).

    Extract commit SHAs from the three Phase 3 SUMMARY files (03-01-SUMMARY.md, 03-02-SUMMARY.md, 03-03-SUMMARY.md) -- look in frontmatter and task commit sections.

    Do NOT fabricate any evidence. Only record what is actually found in the files. If a file does not contain what was expected, note the actual state.
  </action>
  <verify>
    All 11 files read successfully; evidence notes collected for all 6 DATA requirements; commit SHAs extracted from all 3 SUMMARY files.
  </verify>
  <done>Evidence for all 6 DATA requirements is collected from actual source files, not assumed or fabricated.</done>
</task>

<task type="auto">
  <name>Task 2: Write 03-VERIFICATION.md following project format</name>
  <files>.planning/phases/03-airtable-data-layer/03-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/03-airtable-data-layer/03-VERIFICATION.md` following the exact structure from 07-VERIFICATION.md (the format reference).

    The document MUST include these sections:

    **Frontmatter:**
    - phase: 03-airtable-data-layer
    - verified: today's date (ISO)
    - status: human_needed (because live Airtable API calls and cache TTL behavior can only be verified in a running environment)
    - score: 6/6 must-haves verified
    - human_verification: list 2 items (actual Airtable API field mapping against live data; cache TTL behavior in running Next.js app)

    **Goal Achievement / Observable Truths:**
    Use the truth patterns from the research file (09-RESEARCH.md "Observable Truths for DATA Requirements" section). Each truth row must have VERIFIED status with specific file:line or grep evidence from Task 1.

    **Required Artifacts:**
    List all source files and test files from Phase 3 with VERIFIED status and what was checked in each.

    **Key Link Verification:**
    - client.ts -> rate-limiter.ts (rate limiter wraps all Airtable calls)
    - turn-requests.ts -> mappers.ts (resolveLinkedJobs uses buildJobFilterFormula)
    - job-status.ts -> revalidateTag (write operations bust 5 cache tags)

    **Requirements Coverage:**
    Table with DATA-01 through DATA-06, source plan (03-01, 03-02, or 03-03), description, VERIFIED status, and evidence summary.

    **Anti-Patterns Found:**
    Note the 03-UAT.md pre-existing issue (Next.js blocking route error) was fixed in Phase 4 via Suspense wrapping. This is not an unresolved problem.

    **Commits Verified:**
    List all 6 commits from Phase 3 SUMMARY files: 2bb0778, 49c4385, 06fe74d, 901e801, 65abcf2, a19ae6d. Cross-reference these against SUMMARY task descriptions.

    IMPORTANT: Use ONLY evidence gathered in Task 1. Do not invent line numbers, test counts, or file contents.
  </action>
  <verify>
    <automated>grep -c "DATA-0" .planning/phases/03-airtable-data-layer/03-VERIFICATION.md</automated>
  </verify>
  <done>03-VERIFICATION.md exists with all 6 DATA requirements documented as VERIFIED with specific code evidence, commit SHAs listed, and human verification items identified.</done>
</task>

</tasks>

<verification>
- 03-VERIFICATION.md exists at the correct path
- All 6 DATA requirements appear in Observable Truths table with VERIFIED status
- Commits Verified section has 6 entries matching Phase 3 SUMMARY SHAs
- Human Verification section identifies 2 live-environment checks
- No fabricated evidence (all claims traceable to actual file contents)
</verification>

<success_criteria>
Phase 3 has a formal VERIFICATION.md documenting all DATA requirement evidence, matching the project's established verification format, with only evidence-backed claims.
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation-and-verification-cleanup/09-01-SUMMARY.md`
</output>
