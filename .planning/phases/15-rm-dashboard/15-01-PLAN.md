---
phase: 15-rm-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/auth.ts
  - src/lib/__tests__/auth-types.test.ts
  - src/components/layout/sidebar.tsx
  - src/components/layout/bottom-tab-bar.tsx
autonomous: true
requirements: [RMDB-01]

must_haves:
  truths:
    - "RM role routes to /regional on login, not /property"
    - "RM is allowed to access both /regional and /property routes"
    - "/regional appears in sidebar and bottom tab bar for RM users"
    - "Sidebar active state highlights Regional Dashboard for all /regional/* sub-routes"
  artifacts:
    - path: "src/lib/types/auth.ts"
      provides: "ROLE_ROUTES.rm = '/regional' and ROLE_ALLOWED_ROUTES.rm includes '/regional'"
      contains: "rm: '/regional'"
    - path: "src/lib/__tests__/auth-types.test.ts"
      provides: "Updated test assertions for RM routing"
      contains: "expect(ROLE_ROUTES.rm).toBe('/regional')"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Regional Dashboard nav item for RM role"
      contains: "Regional Dashboard"
    - path: "src/components/layout/bottom-tab-bar.tsx"
      provides: "Regional Dashboard tab for RM role on mobile"
      contains: "Regional Dashboard"
  key_links:
    - from: "src/lib/types/auth.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "ROLE_ROUTES and ROLE_ALLOWED_ROUTES consumed by middleware updateSession"
      pattern: "ROLE_ROUTES\\.rm"
    - from: "src/components/layout/sidebar.tsx"
      to: "/regional"
      via: "navItems href for RM role"
      pattern: "href.*regional"
---

<objective>
Update auth routing constants, tests, sidebar, and bottom-tab-bar so RM users are routed to /regional and can navigate there from both desktop and mobile nav.

Purpose: RMDB-01 requires RM middleware routing to /regional. This is the foundation for the dashboard page built in Plan 02.
Output: Updated auth.ts, passing auth tests, sidebar and bottom-tab-bar with Regional Dashboard nav item.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-rm-dashboard/15-CONTEXT.md
@.planning/phases/15-rm-dashboard/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/types/auth.ts:
```typescript
export type UserRole = 'pm' | 'rm' | 'exec'

export const ROLE_ROUTES: Record<UserRole, string> = {
  pm: '/property',
  rm: '/property',     // CHANGE TO: '/regional'
  exec: '/executive',
}

export const ROLE_ALLOWED_ROUTES: Partial<Record<UserRole, string[]>> = {
  exec: ['/executive', '/property', '/vendors', '/vacant'],
  rm: ['/property', '/vendors', '/vacant'],  // ADD: '/regional' at start
  pm: ['/property', '/vendors', '/vacant'],
}
```

From src/components/layout/sidebar.tsx:
```typescript
interface NavItem {
  icon: LucideIcon;
  label: string;
  href: string;
  roles?: UserRole[];
}

const navItems: NavItem[] = [
  { icon: LayoutDashboard, label: "Executive", href: "/executive", roles: ["exec"] },
  { icon: Building2, label: "Properties", href: "/property" },
  // ...
]

// Active check (line 86):
const isActive = activePath === item.href;
```

From src/components/layout/bottom-tab-bar.tsx:
```typescript
const tabItems: TabItem[] = [
  { icon: LayoutDashboard, label: "Executive", href: "/executive", roles: ["exec"] },
  { icon: Building2, label: "Properties", href: "/property" },
  // ...
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update auth constants and tests for RM /regional routing</name>
  <files>src/lib/types/auth.ts, src/lib/__tests__/auth-types.test.ts</files>
  <read_first>
    - src/lib/types/auth.ts (current ROLE_ROUTES and ROLE_ALLOWED_ROUTES values)
    - src/lib/__tests__/auth-types.test.ts (current test assertions to update)
    - src/lib/supabase/middleware.ts (verify how ROLE_ROUTES is consumed — read only, do not modify)
  </read_first>
  <action>
1. In `src/lib/types/auth.ts`:
   - Change `ROLE_ROUTES.rm` from `'/property'` to `'/regional'`
   - Change `ROLE_ALLOWED_ROUTES.rm` from `['/property', '/vendors', '/vacant']` to `['/regional', '/property', '/vendors', '/vacant']`
   - No other changes — UserRole type, AppMetadata, ROLE_LABELS stay the same

2. In `src/lib/__tests__/auth-types.test.ts`:
   - Update test `'ROLE_ROUTES.rm maps to /property'` description to `'ROLE_ROUTES.rm maps to /regional'` and assertion to `expect(ROLE_ROUTES.rm).toBe('/regional')`
   - Update test `'ROLE_ALLOWED_ROUTES.rm includes /property, /vendors, and /vacant'` description to `'ROLE_ALLOWED_ROUTES.rm includes /regional, /property, /vendors, and /vacant'` and assertion to `expect(ROLE_ALLOWED_ROUTES.rm).toEqual(['/regional', '/property', '/vendors', '/vacant'])`
   - All other tests remain unchanged (pm, exec, dm checks, ROLE_LABELS)
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/auth-types.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types/auth.ts contains `rm: '/regional'` in ROLE_ROUTES
    - src/lib/types/auth.ts contains `rm: ['/regional', '/property', '/vendors', '/vacant']` in ROLE_ALLOWED_ROUTES
    - src/lib/__tests__/auth-types.test.ts contains `expect(ROLE_ROUTES.rm).toBe('/regional')`
    - src/lib/__tests__/auth-types.test.ts contains `expect(ROLE_ALLOWED_ROUTES.rm).toEqual(['/regional', '/property', '/vendors', '/vacant'])`
    - All 8 tests in auth-types.test.ts pass
  </acceptance_criteria>
  <done>ROLE_ROUTES.rm is '/regional', ROLE_ALLOWED_ROUTES.rm includes '/regional', all auth-types tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add Regional Dashboard to sidebar and bottom-tab-bar</name>
  <files>src/components/layout/sidebar.tsx, src/components/layout/bottom-tab-bar.tsx</files>
  <read_first>
    - src/components/layout/sidebar.tsx (current navItems array, active state logic at line 86)
    - src/components/layout/bottom-tab-bar.tsx (current tabItems array, active state logic)
    - .planning/phases/15-rm-dashboard/15-UI-SPEC.md (sidebar nav label, icon, active state fix)
  </read_first>
  <action>
1. In `src/components/layout/sidebar.tsx`:
   - Add `Map` to the lucide-react import: `import { Building2, Users, LayoutDashboard, LogOut, UserPlus, DoorOpen, ClipboardCheck, Map } from "lucide-react"`
   - Add new nav item BEFORE the Properties item in the navItems array:
     ```
     { icon: Map, label: "Regional Dashboard", href: "/regional", roles: ["rm"] },
     ```
     Full navItems order: Executive (exec), Regional Dashboard (rm), Properties, Completed Jobs, Vendors, Add Off Market
   - Change the active state check on line 86 from exact match to startsWith:
     ```typescript
     const isActive = activePath.startsWith(item.href);
     ```
     This ensures /regional/property/[id] drill-down still highlights "Regional Dashboard". Safe because all hrefs are distinct path prefixes (/executive, /regional, /property, /property/completed-jobs, /vendors, /vacant).
   - IMPORTANT: `/property/completed-jobs` must be listed BEFORE `/property` in navItems to ensure `startsWith` matches correctly. Currently `Completed Jobs` is at index 2 and `Properties` is at index 1 — swap their order so `Completed Jobs` (href="/property/completed-jobs") comes first, then `Properties` (href="/property"). Otherwise `startsWith('/property')` would match both. Actually, since we iterate all items and the LAST match isn't used (isActive is per-item), both will highlight. Fix: use a more precise check. Change to: `const isActive = item.href === '/property' ? activePath === '/property' : activePath.startsWith(item.href)` — NO, this is over-complex. Simpler: keep exact match `===` as default but add a special case for `/regional`: `const isActive = item.href === '/regional' ? activePath.startsWith('/regional') : activePath === item.href`. This preserves existing behavior for all other items and only adds sub-route matching for the regional dashboard.

2. In `src/components/layout/bottom-tab-bar.tsx`:
   - Add `Map` to the lucide-react import: `import { Building2, Users, LayoutDashboard, UserPlus, DoorOpen, Map } from "lucide-react"`
   - Add new tab item BEFORE the Properties item in the tabItems array:
     ```
     { icon: Map, label: "Regional Dashboard", href: "/regional", roles: ["rm"] },
     ```
     Full tabItems order: Executive (exec), Regional Dashboard (rm), Properties, Vendors, Add Off Market
   - Update the active check (line 68) from `activePath === item.href` to `item.href === '/regional' ? activePath.startsWith('/regional') : activePath === item.href` — same pattern as sidebar
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/sidebar.tsx contains `{ icon: Map, label: "Regional Dashboard", href: "/regional", roles: ["rm"] }`
    - src/components/layout/sidebar.tsx contains `import { Building2, Users, LayoutDashboard, LogOut, UserPlus, DoorOpen, ClipboardCheck, Map } from "lucide-react"`
    - src/components/layout/sidebar.tsx contains `activePath.startsWith('/regional')` for regional active state
    - src/components/layout/bottom-tab-bar.tsx contains `{ icon: Map, label: "Regional Dashboard", href: "/regional", roles: ["rm"] }`
    - src/components/layout/bottom-tab-bar.tsx contains `Map` in lucide-react import
    - Full test suite passes (no regressions in layout tests)
  </acceptance_criteria>
  <done>Sidebar and BottomTabBar show "Regional Dashboard" for RM role, active state works for /regional and /regional/property/* sub-routes, all tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/auth-types.test.ts` — all assertions updated and passing
- `npx vitest run` — full suite green (no regressions)
- `grep -n "rm:.*regional" src/lib/types/auth.ts` — confirms routing constant
- `grep -n "Regional Dashboard" src/components/layout/sidebar.tsx src/components/layout/bottom-tab-bar.tsx` — confirms nav items
</verification>

<success_criteria>
- ROLE_ROUTES.rm === '/regional'
- ROLE_ALLOWED_ROUTES.rm includes '/regional', '/property', '/vendors', '/vacant'
- Sidebar shows "Regional Dashboard" nav item for RM role with Map icon
- BottomTabBar shows "Regional Dashboard" tab for RM role with Map icon
- Active state on sidebar/bottom-tab-bar works for /regional and all /regional/* sub-routes
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/15-rm-dashboard/15-01-SUMMARY.md`
</output>
