---
phase: 04-executive-dashboard
plan: 03
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - src/app/(dashboard)/executive/page.tsx
  - src/app/(dashboard)/executive/_components/executive-kpis.tsx
  - src/components/layout/app-shell.tsx
  - src/components/ui/kpi-card.tsx
autonomous: false
requirements: [EXEC-01, EXEC-07]
gap_closure: true

must_haves:
  truths:
    - "All text on the executive dashboard is readable against the dark green background"
    - "The entire executive dashboard fits within one viewport on a standard desktop (no scrolling needed)"
    - "KPI cards, headings, and alert lists all use light text colors that contrast with the dark green body"
  artifacts:
    - path: "src/app/(dashboard)/executive/page.tsx"
      provides: "Page title moved to AppShell header slot, no standalone title block"
    - path: "src/components/layout/app-shell.tsx"
      provides: "Dynamic page title slot replacing static Overview text"
    - path: "src/app/(dashboard)/executive/_components/executive-kpis.tsx"
      provides: "Light-colored text for Make Ready heading and alert list items"
    - path: "src/components/ui/kpi-card.tsx"
      provides: "Reduced padding for compact card layout"
  key_links:
    - from: "src/app/(dashboard)/executive/page.tsx"
      to: "src/components/layout/app-shell.tsx"
      via: "pageTitle prop passed through layout to AppShell"
      pattern: "pageTitle.*Executive Dashboard"
---

<objective>
Fix two UAT cosmetic issues on the executive dashboard: (1) dark text invisible on dark green background, (2) content overflows viewport due to duplicate header and excessive spacing.

Purpose: User reported "Those are very dark; I can't see any of them" and requested everything fit in one viewport by removing the redundant Overview header and tightening spacing.
Output: Readable executive dashboard that fits in a single desktop viewport.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-executive-dashboard/04-02-SUMMARY.md
@.planning/phases/04-executive-dashboard/04-UAT.md

<interfaces>
<!-- Current AppShell signature -->
From src/components/layout/app-shell.tsx:
```typescript
export function AppShell({
  children,
  userHeader,
}: {
  children: React.ReactNode;
  userHeader?: React.ReactNode;
})
```

<!-- Current KPICard props -->
From src/components/ui/kpi-card.tsx:
```typescript
export interface KPICardProps {
  icon: LucideIcon;
  label: string;
  value: string | number;
  trend?: { direction: "up" | "down"; percentage: number };
  variant?: "default" | "highlighted" | "alert-past" | "alert-trending";
  loading?: boolean;
  className?: string;
}
```

<!-- Key Tailwind tokens (from THEME.md / CSS) -->
- text-text-primary: #111827 (dark gray -- invisible on dark green)
- text-text-secondary: #6B7280 (medium gray -- invisible on dark green)
- bg-forest: dark green body background
- text-white: #FFFFFF
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix text contrast and reduce spacing for single-viewport fit</name>
  <files>
    src/app/(dashboard)/executive/page.tsx,
    src/app/(dashboard)/executive/_components/executive-kpis.tsx,
    src/components/layout/app-shell.tsx,
    src/components/ui/kpi-card.tsx
  </files>
  <action>
**Text contrast fixes (Gap 1):**

1. In `src/app/(dashboard)/executive/page.tsx`:
   - Change h1 class from `text-text-primary` to `text-white`
   - Change subtitle p class from `text-text-secondary` to `text-white/70`

2. In `src/app/(dashboard)/executive/_components/executive-kpis.tsx`:
   - Change the Make Ready Overview h2 from `text-text-primary` to `text-white`
   - Change AlertItemList ul from `text-text-secondary` to `text-white/70`

**Vertical space reduction (Gap 2):**

3. In `src/components/layout/app-shell.tsx`:
   - Remove the static "Overview" h1 and its dot-grid icon container entirely (the div at lines 28-36 containing the grid icon and the h1 "Overview"). This element serves no functional purpose on any page and consumes ~40px of vertical space. Keep the rest of the header (search, mail, bell, userHeader icons).

4. In `src/app/(dashboard)/executive/page.tsx`:
   - Change the outer div from `gap-6` to `gap-3`
   - Change h1 from `text-2xl` to `text-lg` and use `text-white` (already done in step 1)
   - Change subtitle from `text-sm mt-1` to `text-xs mt-0.5` and use `text-white/70` (already done in step 1)

5. In `src/app/(dashboard)/executive/_components/executive-kpis.tsx`:
   - Change outer div from `gap-6` to `gap-4`
   - Change Make Ready Overview `mb-4` to `mb-2`

6. In `src/components/ui/kpi-card.tsx`:
   - Change `p-6` to `p-4` in both the loading skeleton and the main card render (two places: line 36 and line 52)

**IMPORTANT:** Do NOT change KPICard internal text colors (text-text-primary, text-text-secondary inside cards). Cards have white backgrounds (`bg-card`), so dark text is correct INSIDE cards. Only change text that renders directly on the dark green page background.
  </action>
  <verify>
    <automated>cd C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions && npx tsc --noEmit && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All text rendered on dark green background uses white or white/70 (not text-text-primary or text-text-secondary)
    - KPI card text inside white cards still uses dark text (unchanged)
    - Static "Overview" heading removed from AppShell header
    - Page title "Executive Dashboard" shown in page.tsx with text-white text-lg
    - Spacing reduced: gap-3 on page, gap-4 on KPI container, p-4 on cards, mb-2 on Make Ready heading
    - TypeScript compiles, all tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of contrast and layout fixes</name>
  <files>src/app/(dashboard)/executive/page.tsx</files>
  <action>User visually verifies that text contrast and vertical spacing fixes resolve the reported UAT issues.</action>
  <verify>
    1. Navigate to http://localhost:3000/executive (or your dev URL)
    2. Verify ALL text is readable:
       - "Executive Dashboard" heading is white and visible
       - "Welcome, [name] -- [date]" subtitle is visible (white with slight transparency)
       - "Make Ready Overview" heading is white and visible
       - Alert list items (if any) are visible with light text
    3. Verify the dashboard fits in one viewport on your screen without scrolling
    4. Verify KPI cards still have dark text INSIDE the white card backgrounds
    5. Verify the old "Overview" static text is gone from the top header bar
  </verify>
  <done>User confirms dashboard is readable and fits in one viewport. Type "approved" or describe remaining issues.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- All existing tests pass: `npx vitest run`
- No text-text-primary or text-text-secondary classes remain on elements rendered directly on the dark green background in executive page files
- KPICard p-4 padding applied (was p-6)
- Gap reduced from gap-6 to gap-4 in executive-kpis.tsx
</verification>

<success_criteria>
- User confirms all dashboard text is readable against dark green background
- User confirms dashboard fits in one desktop viewport without scrolling
- All 96+ existing tests continue to pass
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-executive-dashboard/04-03-SUMMARY.md`
</output>
