---
phase: 06-district-manager-view
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified: [".planning/REQUIREMENTS.md"]
autonomous: true
gap_closure: true
requirements: [DM-01, DM-02, DM-03, DM-04]

must_haves:
  truths:
    - "DM-01 through DM-04 descriptions in REQUIREMENTS.md accurately describe what Phase 6 delivered (rm rename, routing update, label update, redirect)"
    - "Traceability table entries for DM-01 through DM-04 remain marked Complete under Phase 6"
    - "A rationale note explains why the original District Manager portfolio UI was dropped in favour of reusing the PM view"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated DM requirement descriptions matching delivered outcomes"
      contains: "rm"
  key_links: []
---

<objective>
Update REQUIREMENTS.md DM-01 through DM-04 to describe the actual rm rename outcomes delivered in Phase 6, replacing the original District Manager portfolio UI descriptions that were intentionally dropped.

Purpose: Close the documentation gap identified in 06-VERIFICATION.md — requirement descriptions must match delivered work so the traceability table remains accurate for auditing.
Output: Updated REQUIREMENTS.md with rewritten DM-01 through DM-04 descriptions.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-district-manager-view/06-CONTEXT.md
@.planning/phases/06-district-manager-view/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DM-01 through DM-04 requirement descriptions in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read .planning/REQUIREMENTS.md. In the "District Manager View" section, rewrite the four requirement descriptions to match what Phase 6 actually delivered:

- **DM-01**: Change from "Portfolio overview with one card per assigned property showing: property name, active turns, completion rate, pending approvals" to: "UserRole type contains 'rm' (Regional Manager) replacing 'dm'; ROLE_ROUTES maps rm to /property; ROLE_LABELS maps rm to 'Regional Manager'"
- **DM-02**: Change from "KPI row: Turn completion rate (gauge), Jobs pending approval, Overdue items (alert styling if > 0)" to: "RM users see the same PM KPI cards and turn list via shared /property route — no separate KPI view needed"
- **DM-03**: Change from "Click property card to drill-down into that property's data (reuses PM turn list and KPI components)" to: "RM users with multiple properties see PropertySelector dropdown in header for multi-property access (reuses existing PM components)"
- **DM-04**: Change from "Loading skeleton states" to: "/district URL redirects to /property gracefully; all existing tests pass and TypeScript compiles with zero errors after rename"

Keep the [x] checkmarks. Keep the **DM-XX** bold ID format. Keep the section header as "District Manager View" but add a note line immediately after the header:

> *Redesigned in Phase 6: Regional Managers reuse the PM view with PropertySelector for multi-property access. The original portfolio UI was dropped per user decision (see 06-CONTEXT.md).*

Do NOT change any other section of REQUIREMENTS.md. Do NOT modify the traceability table — it already correctly shows DM-01 through DM-04 as Complete under Phase 6.
  </action>
  <verify>
    <automated>grep -A 1 "DM-01" .planning/REQUIREMENTS.md | grep -q "rm" && grep -A 1 "DM-04" .planning/REQUIREMENTS.md | grep -q "redirect" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>DM-01 through DM-04 descriptions in REQUIREMENTS.md accurately describe the rm rename, shared PM view, PropertySelector dropdown, and /district redirect delivered in Phase 6. A rationale note explains the design change.</done>
</task>

</tasks>

<verification>
- DM-01 mentions 'rm' role type rename
- DM-02 mentions shared PM view / KPI cards
- DM-03 mentions PropertySelector dropdown
- DM-04 mentions /district redirect and test/compile verification
- Rationale note references Phase 6 context decision
- No other sections of REQUIREMENTS.md were modified
- Traceability table unchanged (DM-01 through DM-04 still Complete under Phase 6)
</verification>

<success_criteria>
REQUIREMENTS.md DM-01 through DM-04 descriptions match the actual Phase 6 deliverables. A reader auditing the requirements document can understand what was built and why the original portfolio UI was dropped.
</success_criteria>

<output>
After completion, create `.planning/phases/06-district-manager-view/06-02-SUMMARY.md`
</output>
