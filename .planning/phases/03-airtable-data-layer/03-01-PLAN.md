---
phase: 03-airtable-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - .env.local.example
  - src/lib/types/airtable.ts
  - src/lib/airtable/rate-limiter.ts
  - src/lib/airtable/client.ts
  - src/lib/airtable/cache-tags.ts
  - src/lib/airtable/__tests__/rate-limiter.test.ts
  - src/lib/airtable/__tests__/client.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "Airtable API key is server-side only — no NEXT_PUBLIC_ prefix"
    - "Next.js caching is enabled with cacheComponents and airtableData profile"
    - "Rate limiter prevents more than 5 requests per second"
  artifacts:
    - path: "src/lib/types/airtable.ts"
      provides: "All 9 TypeScript interfaces + JobStatus enum"
      exports: ["TurnRequest", "Job", "Property", "Vendor", "VendorPricing", "Quote", "Executive", "PropertyManager", "MaintenanceManager", "JobStatus", "JOB_STATUSES"]
    - path: "src/lib/airtable/client.ts"
      provides: "Airtable base singleton + rateLimiter re-export"
      exports: ["base", "rateLimiter"]
    - path: "src/lib/airtable/rate-limiter.ts"
      provides: "Token bucket rate limiter"
      exports: ["TokenBucket", "rateLimiter"]
    - path: "src/lib/airtable/cache-tags.ts"
      provides: "Cache tag constants and builders"
      exports: ["CACHE_TAGS"]
    - path: "next.config.ts"
      provides: "cacheComponents: true + airtableData cache profile"
      contains: "cacheComponents"
  key_links:
    - from: "src/lib/airtable/client.ts"
      to: "src/lib/airtable/rate-limiter.ts"
      via: "import { rateLimiter }"
      pattern: "import.*rateLimiter.*rate-limiter"
    - from: "src/lib/airtable/client.ts"
      to: "process.env.AIRTABLE_API_KEY"
      via: "env var guard"
      pattern: "process\\.env\\.AIRTABLE_API_KEY"
---

<objective>
Set up the Airtable data layer foundation: install dependencies, enable Next.js caching, define all TypeScript interfaces, build the rate limiter, create the Airtable client singleton, and define cache tag constants.

Purpose: Every subsequent plan depends on these types, client, and infrastructure. This is the contract layer.
Output: Working Airtable client with rate limiting, all types defined, caching enabled.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-airtable-data-layer/03-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs -->

From src/lib/types/auth.ts:
```typescript
export type UserRole = 'pm' | 'dm' | 'exec'
export interface AppMetadata {
  role: UserRole
  property_ids?: string[]
}
```

From src/lib/normalize-property-name.ts:
```typescript
export function filterByProperties<T>(
  items: T[],
  getPropertyName: (item: T) => string,
  assignedNames: string[]
): T[]
```

From next.config.ts (current — needs modification):
```typescript
import type { NextConfig } from "next";
const nextConfig: NextConfig = { /* config options here */ };
export default nextConfig;
```

From .env.local.example (current — needs AIRTABLE vars added):
```
NEXT_PUBLIC_SUPABASE_URL=https://supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=TEST
SUPABASE_SERVICE_ROLE_KEY=TEST
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, enable caching, define types and cache tags</name>
  <files>next.config.ts, .env.local.example, src/lib/types/airtable.ts, src/lib/airtable/cache-tags.ts</files>
  <behavior>
    - next.config.ts has `cacheComponents: true` and `cacheLife.airtableData` with stale:60, revalidate:60, expire:300
    - .env.local.example includes AIRTABLE_API_KEY and AIRTABLE_BASE_ID (no NEXT_PUBLIC_ prefix)
    - airtable.ts exports all 9 interfaces (TurnRequest, Job, Property, Vendor, VendorPricing, Quote, Executive, PropertyManager, MaintenanceManager) plus JOB_STATUSES const and JobStatus type
    - cache-tags.ts exports CACHE_TAGS with table-level tags and record-level tag builders
  </behavior>
  <action>
    1. Run `npm install airtable sonner` to add Airtable SDK and toast library.
    2. Update `next.config.ts`: add `cacheComponents: true` and a `cacheLife` object with the `airtableData` custom profile (`stale: 60, revalidate: 60, expire: 300`). Keep the existing config structure.
    3. Update `.env.local.example`: add `AIRTABLE_API_KEY=your_airtable_api_key` and `AIRTABLE_BASE_ID=your_airtable_base_id` (no NEXT_PUBLIC_ prefix — DATA-01).
    4. Create `src/lib/types/airtable.ts` with all 9 TypeScript interfaces and the `JOB_STATUSES` const object + `JobStatus` type. Use exact field mappings from the RESEARCH.md "Airtable Field Mapping" section. Key points:
       - `TurnRequest.jobIds` is `number[]` (parsed from comma-separated string), `jobs` is optional `Job[]` (resolved linked records)
       - Currency fields (`totalCost`, `value`) are `string | null`
       - `Job.status` is typed as `JobStatus`
       - `Job.quotePrice` is `number | null`
    5. Create `src/lib/airtable/cache-tags.ts` with `CACHE_TAGS` const. Table-level tags: turnRequests, jobs, properties, vendors, vendorPricing, quotes, kpis. Record-level builders: `turnRequest(id: number)`, `job(id: number)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/types/airtable.ts src/lib/airtable/cache-tags.ts 2>&1 | head -20</automated>
  </verify>
  <done>All 9 Airtable interfaces exported with correct field types, JOB_STATUSES enum defined, cache tags centralized, caching enabled in next.config.ts, env vars documented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build rate limiter and Airtable client with tests</name>
  <files>src/lib/airtable/rate-limiter.ts, src/lib/airtable/client.ts, src/lib/airtable/__tests__/rate-limiter.test.ts, src/lib/airtable/__tests__/client.test.ts</files>
  <behavior>
    - TokenBucket with capacity=5, refillRate=5/1000 acquires at most 5 tokens instantly, then blocks
    - TokenBucket.acquire() returns Promise<void> that resolves when a token is available
    - Under burst of 10 rapid calls, first 5 resolve immediately, next 5 are spaced ~200ms apart
    - client.ts throws if AIRTABLE_API_KEY or AIRTABLE_BASE_ID env vars are missing
    - client.ts exports `base` (Airtable base instance) and re-exports `rateLimiter`
    - No env var uses NEXT_PUBLIC_ prefix (DATA-01)
  </behavior>
  <action>
    1. Create `src/lib/airtable/rate-limiter.ts` implementing the `TokenBucket` class from RESEARCH.md Pattern 2. Export both the class and a singleton instance `rateLimiter = new TokenBucket(5, 5 / 1000)`. Add console.log for rate-limit events: log when a request is queued (waiting for token).
    2. Create `src/lib/airtable/client.ts` following RESEARCH.md "Airtable Client Singleton" example. Guard both `AIRTABLE_API_KEY` and `AIRTABLE_BASE_ID` with runtime checks that throw if missing. Export `base` and re-export `rateLimiter`.
    3. Create `src/lib/airtable/__tests__/rate-limiter.test.ts`:
       - Test: "allows up to 5 immediate acquisitions" — acquire 5 times, all resolve within 50ms
       - Test: "delays 6th acquisition" — acquire 6 times, 6th takes >150ms
       - Test: "refills tokens over time" — acquire 5, wait 1100ms, acquire 5 more (all immediate)
    4. Create `src/lib/airtable/__tests__/client.test.ts`:
       - Test: "throws if AIRTABLE_API_KEY is not set" — unset env var, dynamic import, expect throw
       - Test: "throws if AIRTABLE_BASE_ID is not set" — unset env var, dynamic import, expect throw
       - Test: "does not use NEXT_PUBLIC_ prefix" — grep source file for NEXT_PUBLIC_ and assert zero matches
  </action>
  <verify>
    <automated>npx vitest run src/lib/airtable/__tests__/rate-limiter.test.ts src/lib/airtable/__tests__/client.test.ts --reporter=verbose</automated>
  </verify>
  <done>Rate limiter throttles to 5 req/sec with token bucket algorithm. Client singleton guards env vars and exports base instance. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/airtable/__tests__/ --reporter=verbose` — all tests pass
- `npx tsc --noEmit` — no type errors
- `grep -r "NEXT_PUBLIC_AIRTABLE" src/` returns zero results (DATA-01)
- `grep "cacheComponents" next.config.ts` returns a match (DATA-03)
</verification>

<success_criteria>
- All 9 TypeScript interfaces defined and exported from src/lib/types/airtable.ts
- JOB_STATUSES const with 5 valid statuses and JobStatus type exported
- Rate limiter tests prove 5 req/sec throttling
- Airtable client guards env vars at module load
- next.config.ts has cacheComponents: true and airtableData cache profile
- No NEXT_PUBLIC_AIRTABLE_* env vars anywhere in src/
</success_criteria>

<output>
After completion, create `.planning/phases/03-airtable-data-layer/03-01-SUMMARY.md`
</output>
