---
phase: 01-scaffolding-and-design-system
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/app/components/page.tsx
  - vitest.config.ts
  - tests/setup.ts
  - src/components/ui/__tests__/components.test.tsx
  - src/components/layout/__tests__/layout.test.tsx
autonomous: false
requirements:
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Dev demo page at /components shows all 10 components with sample data"
    - "All component unit tests pass"
    - "Layout tests verify sidebar and bottom tab bar render"
    - "Visual inspection confirms THEME.md styling is correct"
  artifacts:
    - path: "src/app/components/page.tsx"
      provides: "Demo page showcasing all 10 UI components"
      min_lines: 50
    - path: "vitest.config.ts"
      provides: "Vitest configuration for Next.js component testing"
      contains: "react"
    - path: "tests/setup.ts"
      provides: "Testing Library setup with custom matchers"
      contains: "testing-library"
    - path: "src/components/ui/__tests__/components.test.tsx"
      provides: "Unit tests for all 10 UI components"
      min_lines: 50
    - path: "src/components/layout/__tests__/layout.test.tsx"
      provides: "Unit tests for layout components"
      min_lines: 20
  key_links:
    - from: "src/app/components/page.tsx"
      to: "src/components/ui/index.ts"
      via: "imports all components from barrel export"
      pattern: "@/components/ui"
    - from: "vitest.config.ts"
      to: "tests/setup.ts"
      via: "setupFiles configuration"
      pattern: "setup"
---

<objective>
Create a dev-only demo page at /components showcasing all 10 UI components with sample data, set up Vitest with React Testing Library, and write unit tests for all components and layout. Conclude with a visual verification checkpoint.

Purpose: Validate that all components render correctly, are properly themed, and behave as specified before moving to Phase 2.
Output: Demo page for visual validation, passing test suite, verified design system.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffolding-and-design-system/01-CONTEXT.md
@.planning/phases/01-scaffolding-and-design-system/01-RESEARCH.md
@.planning/phases/01-scaffolding-and-design-system/01-01-SUMMARY.md
@.planning/phases/01-scaffolding-and-design-system/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — all components available via barrel export -->
From src/components/ui/index.ts:
```typescript
export { Button } from "./button";
export { Card } from "./card";
export { KPICard } from "./kpi-card";
export { Badge } from "./badge";
export { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from "./table";
export { Input } from "./input";
export { Skeleton } from "./skeleton";
export { StatusBadge } from "./status-badge";
export { TrendIndicator } from "./trend-indicator";
export { CurrencyDisplay } from "./currency-display";
```

From src/components/layout/:
```typescript
// sidebar.tsx
export function Sidebar({ activePath }: { activePath: string }): JSX.Element;
// bottom-tab-bar.tsx
export function BottomTabBar({ activePath }: { activePath: string }): JSX.Element;
// app-shell.tsx
export function AppShell({ children }: { children: React.ReactNode }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo page and test infrastructure</name>
  <files>src/app/components/page.tsx, vitest.config.ts, tests/setup.ts, src/components/ui/__tests__/components.test.tsx, src/components/layout/__tests__/layout.test.tsx</files>
  <action>
1. Install test dependencies: `npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom jsdom`

2. Create `vitest.config.ts`:
   - Import defineConfig from vitest/config and react plugin from @vitejs/plugin-react
   - Configure: plugins [react()], test environment "jsdom", setupFiles ["./tests/setup.ts"], globals true
   - Add path alias: resolve.alias with "@" pointing to "./src"

3. Create `tests/setup.ts`:
   - Import "@testing-library/jest-dom/vitest" for custom matchers (toBeInTheDocument, etc.)
   - Add any global cleanup if needed

4. Create `src/app/components/page.tsx` — dev-only demo page showcasing ALL 10 components:
   - Import all components from "@/components/ui"
   - Import sample icons from lucide-react (Activity, DollarSign, Clock, AlertTriangle, TrendingUp, Building2)
   - Page title: "Component Library" in font-heading text-3xl text-white mb-8
   - Sections for each component group, each with a heading and examples:

   **Buttons section:** Show all 4 variants (primary, secondary, ghost, cta) in all 3 sizes

   **Cards section:** Default Card with some text content, flush Card variant

   **KPI Cards section:** Show 4 KPICards in a responsive grid (grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4):
     - Default with trend up: icon=Activity, label="Active Make Readys", value=12, trend={direction:"up", percentage:8.5}
     - Default with trend down: icon=Clock, label="Avg Completion Time", value="4.2 days", trend={direction:"down", percentage:3.1}
     - Alert past target: icon=AlertTriangle, label="Past Target Time", value=3, variant="alert-past"
     - Loading state: loading=true with any placeholder props

   **Badges section:** All 3 variants (default, emerald, outline)

   **StatusBadges section:** All 5 statuses in a flex row with gap-2

   **Input section:** Default input, input with label, input with error state

   **Table section:** Sample table with 4 rows of mock turn data:
     - Columns: Property, Unit, Status, Ready Date, Price
     - Use StatusBadge in the Status column
     - Use CurrencyDisplay in the Price column

   **TrendIndicator section:** Up and down examples side by side

   **CurrencyDisplay section:** Show formatted values: 0, 1500, 25000, 1234567

   **Skeleton section:** Various skeleton sizes demonstrating the component

5. Create `src/components/ui/__tests__/components.test.tsx`:
   - Import render, screen from @testing-library/react
   - Import all components from "@/components/ui"
   - Tests for each component:
     - Button: renders with text, applies variant classes, handles click
     - Card: renders children
     - Badge: renders with text, applies variant
     - Input: renders, accepts value, shows label, shows error
     - Skeleton: renders with aria-hidden
     - StatusBadge: renders correct label for each status, renders with correct role
     - TrendIndicator: renders percentage, shows correct icon direction
     - CurrencyDisplay: formats 1500 as "$1,500", formats 0 as "$0"
     - KPICard: renders label and value, renders skeleton when loading=true (check for aria-hidden elements), renders alert variant
     - Table compound: renders with header and rows

6. Create `src/components/layout/__tests__/layout.test.tsx`:
   - Mock next/navigation usePathname to return "/"
   - Test Sidebar renders navigation links
   - Test BottomTabBar renders navigation links
   - Test AppShell renders children content

7. Add test script to package.json if not present: `"test": "vitest run --reporter=verbose"`
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Demo page exists at /components showing all 10 components with sample data. All unit tests pass. Test infrastructure (Vitest + RTL) is configured and working.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete design system</name>
  <action>
  User visually verifies the complete design system: 10 UI components themed per THEME.md, responsive layout shell (sidebar + bottom tab bar), and dev demo page showcasing everything.
  </action>
  <what-built>Complete design system: 10 UI components themed per THEME.md, responsive layout shell (sidebar + bottom tab bar), and dev demo page showcasing everything</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project root
    2. Visit http://localhost:3000 — verify forest green background with white card placeholder, sidebar icons on left
    3. Visit http://localhost:3000/components — verify all 10 components render with correct styling:
       - Buttons: 4 variants x 3 sizes, emerald primary, chartreuse CTA
       - KPI Cards: icon badge, bold number, trend arrows, pink/yellow alert variants, skeleton loading state
       - StatusBadges: green/red/yellow/blue pills, yellow has dark text (not white)
       - Table: subtle row dividers, green-tint hover, no heavy borders
       - Input: clean with label and error states
    4. Hover sidebar icons — tooltips should appear to the right
    5. Resize browser to mobile width (<768px) — sidebar should disappear, floating bottom tab bar should appear at bottom with rounded corners
    6. Verify fonts: headings use Plus Jakarta Sans (rounded geometric), body uses Geist (clean neutral)
  </how-to-verify>
  <verify>Visual confirmation by user</verify>
  <done>User has approved the design system visually. Phase 1 is complete.</done>
  <resume-signal>Type "approved" to complete Phase 1, or describe any visual/behavioral issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass
- `npm run build` — no build errors
- Demo page renders all 10 components with sample data
- Visual inspection confirms THEME.md compliance
</verification>

<success_criteria>
- All unit tests pass (components + layout)
- Demo page at /components showcases all 10 components
- Visual verification confirms theme colors, fonts, responsive behavior
- Phase 1 design system is complete and ready for Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-and-design-system/01-03-SUMMARY.md`
</output>
