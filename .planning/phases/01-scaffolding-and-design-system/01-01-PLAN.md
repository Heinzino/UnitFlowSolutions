---
phase: 01-scaffolding-and-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/utils.ts
  - src/components/layout/sidebar.tsx
  - src/components/layout/bottom-tab-bar.tsx
  - src/components/layout/app-shell.tsx
  - postcss.config.mjs
  - tsconfig.json
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Application loads in browser with forest green background (#0F4A2A)"
    - "Plus Jakarta Sans renders on heading elements"
    - "Geist renders on body text elements"
    - "Desktop viewport shows icon sidebar on the left and main content area on the right"
    - "Mobile viewport hides sidebar and shows floating bottom tab bar"
    - "Sidebar icons have tooltips on hover"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme config with all THEME.md design tokens"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with font loading and AppShell"
      contains: "Plus_Jakarta_Sans"
    - path: "src/components/layout/app-shell.tsx"
      provides: "Two-column layout shell with responsive behavior"
      min_lines: 15
    - path: "src/components/layout/sidebar.tsx"
      provides: "Icon-only sidebar with Radix tooltips"
      contains: "Tooltip"
    - path: "src/components/layout/bottom-tab-bar.tsx"
      provides: "Mobile floating tab bar"
      min_lines: 15
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/app-shell.tsx"
      via: "import and render AppShell wrapping children"
      pattern: "AppShell"
    - from: "src/components/layout/app-shell.tsx"
      to: "src/components/layout/sidebar.tsx"
      via: "import Sidebar for desktop view"
      pattern: "Sidebar"
    - from: "src/components/layout/app-shell.tsx"
      to: "src/components/layout/bottom-tab-bar.tsx"
      via: "import BottomTabBar for mobile view"
      pattern: "BottomTabBar"
    - from: "src/app/globals.css"
      to: "src/app/layout.tsx"
      via: "CSS import provides theme tokens used by Tailwind classes"
      pattern: "globals.css"
---

<objective>
Scaffold the Next.js 16 project, configure the Tailwind v4 theme with all THEME.md design tokens, set up fonts (Plus Jakarta Sans + Geist), and build the responsive layout shell (icon sidebar on desktop, floating bottom tab bar on mobile).

Purpose: Establish the visual foundation and project structure that every subsequent phase builds on. Without this, no component or view can be themed or laid out correctly.
Output: A running Next.js app with themed layout shell, ready to receive UI components.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding-and-design-system/01-CONTEXT.md
@.planning/phases/01-scaffolding-and-design-system/01-RESEARCH.md
@THEME.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and configure Tailwind v4 theme</name>
  <files>package.json, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/lib/utils.ts, postcss.config.mjs, tsconfig.json</files>
  <action>
1. Run `npx create-next-app@latest . --typescript --app --tailwind --eslint --src-dir --import-alias "@/*"` to scaffold in the current directory. If the command asks about overwriting existing files, accept. If it fails because files exist, scaffold into a temp directory and move files.

2. Install dependencies: `npm install radix-ui lucide-react geist clsx`

3. Verify Tailwind version in package.json. If v3, upgrade: `npm install tailwindcss@latest @tailwindcss/postcss@latest postcss@latest` and delete any tailwind.config.js/ts file. Ensure postcss.config.mjs uses `@tailwindcss/postcss` plugin.

4. Configure `src/app/globals.css` with Tailwind v4 CSS-first theme. Use `@import "tailwindcss";` then `@theme { ... }` block defining ALL THEME.md tokens:
   - Colors: `--color-forest: #0F4A2A`, `--color-forest-light: #1a6b3d`, `--color-emerald: #22C55E`, `--color-emerald-dark: #16A34A`, `--color-chartreuse: #CAEF45`, `--color-card: #FFFFFF`, `--color-text-primary: #111827`, `--color-text-secondary: #6B7280`, `--color-positive: #22C55E`, `--color-negative: #EF4444`, `--color-link: #14B8A6`, `--color-status-ready: #22C55E`, `--color-status-attention: #EF4444`, `--color-status-blocked: #EAB308`, `--color-status-progress: #3B82F6`, `--color-alert-past-target: #FDE2E7`, `--color-alert-trending: #FEF3C7`
   - Typography: `--font-heading: var(--font-plus-jakarta-sans)`, `--font-body: var(--font-geist-sans)`
   - Spacing/radius: `--radius-card: 16px`, `--radius-pill: 9999px`, `--radius-badge: 8px`

5. Configure `src/app/layout.tsx`:
   - Import Plus_Jakarta_Sans from `next/font/google` with weights 400,500,600,700,800 and variable `--font-plus-jakarta-sans`
   - Import GeistSans from `geist/font/sans` — log the `.variable` property value during dev to confirm the CSS variable name, then match it in the @theme `--font-body` definition
   - Apply both font variables to the `<html>` className
   - Set body className to `bg-forest text-text-primary font-body antialiased`
   - Add metadata: title "ScheduleSimple" and description

6. Create `src/lib/utils.ts` with a `cn()` helper function using clsx for conditional class merging.

7. Update `src/app/page.tsx` to render a simple placeholder: a white card on the forest green background with the text "ScheduleSimple" in heading font and "Dashboard loading..." in body font. This validates that theme colors and fonts are working.
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Project builds successfully. globals.css contains all THEME.md tokens in @theme block. layout.tsx loads both fonts. Placeholder page renders with forest green background and white card.</done>
</task>

<task type="auto">
  <name>Task 2: Build responsive layout shell with sidebar and bottom tab bar</name>
  <files>src/components/layout/sidebar.tsx, src/components/layout/bottom-tab-bar.tsx, src/components/layout/app-shell.tsx, src/app/layout.tsx</files>
  <action>
1. Create `src/components/layout/sidebar.tsx`:
   - Icon-only sidebar with NO text labels (per user decision)
   - Universal icon set for all roles: Dashboard (LayoutDashboard), Properties (Building2), Vendors (Users), Notifications (Bell), Settings (Settings), Logout (LogOut) — all from lucide-react
   - Each icon wrapped in Radix UI Tooltip (import from `radix-ui` package using namespace: `import { Tooltip } from "radix-ui"`) showing the label on hover, positioned to the right with sideOffset={8}
   - Active state: `bg-emerald text-white` filled circle background (w-10 h-10 rounded-full)
   - Inactive state: `text-white/60 hover:text-white hover:bg-white/10` (w-10 h-10 rounded-full)
   - Accept `activePath` prop to determine which icon is active (match against href)
   - Wrap entire sidebar in a single `Tooltip.Provider` with delayDuration={200}
   - Tooltip content styled: `bg-text-primary text-white text-sm px-3 py-1.5 rounded-badge shadow-lg` with Tooltip.Arrow
   - Navigation items: Dashboard="/", Properties="/property", Vendors="/vendors", Notifications="/notifications", Settings="/settings"
   - Logout item at bottom of sidebar (use mt-auto to push to bottom), no href — just a button for now

2. Create `src/components/layout/bottom-tab-bar.tsx`:
   - Mobile floating tab bar — same icons as sidebar (Dashboard, Properties, Vendors, Notifications, Settings) but NO Logout
   - Styled like iOS tab bar: fixed bottom-4 left-4 right-4, rounded-2xl, bg-forest/95 backdrop-blur-sm, shadow-2xl
   - Active tab: `bg-emerald text-white` filled circle highlight
   - Inactive tab: `text-white/60`
   - Accept `activePath` prop same as sidebar
   - Each icon is w-10 h-10 flex centered, icons at size={20}

3. Create `src/components/layout/app-shell.tsx`:
   - Two-column layout: sidebar (fixed left, w-16) + main content area
   - Desktop/tablet (md: and up): sidebar visible on left, main content offset with `md:ml-16`
   - Mobile (below md): sidebar hidden, bottom tab bar visible
   - Main content area: `p-4 md:p-6 lg:p-8 pb-24 md:pb-6` (extra bottom padding on mobile for tab bar clearance — addresses Pitfall 4)
   - Use `"use client"` directive since it needs `usePathname()` from `next/navigation` to determine activePath
   - Pass activePath to both Sidebar and BottomTabBar

4. Update `src/app/layout.tsx` to wrap `{children}` in `<AppShell>`.
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>AppShell renders two-column layout on desktop (sidebar + main). Sidebar shows 6 icons with Radix tooltips. Mobile viewport shows floating bottom tab bar with 5 icons. Active state shows emerald circle background. Main content has bottom padding on mobile to clear tab bar.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run dev` shows forest green background with white card placeholder
- Sidebar icons are visible at desktop width with tooltip on hover
- Bottom tab bar appears only on mobile viewport
- Plus Jakarta Sans loads for heading text, Geist for body text (verify in browser DevTools)
</verification>

<success_criteria>
- Application builds and runs with THEME.md colors applied (forest green bg, white cards, emerald accents)
- Two-column layout on desktop/tablet, bottom tab bar on mobile
- Both fonts load correctly (Plus Jakarta Sans headings, Geist body)
- All theme tokens accessible as Tailwind utility classes (bg-forest, text-emerald, rounded-card, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-and-design-system/01-01-SUMMARY.md`
</output>
