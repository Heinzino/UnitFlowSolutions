---
phase: 01-scaffolding-and-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/input.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/status-badge.tsx
  - src/components/ui/trend-indicator.tsx
  - src/components/ui/currency-display.tsx
  - src/components/ui/kpi-card.tsx
  - src/components/ui/table.tsx
  - src/components/ui/index.ts
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "All 10 components render without errors when imported and used"
    - "StatusBadge shows correct fill colors per status (green/red/yellow/blue) with appropriate text contrast"
    - "KPICard renders loading skeleton when loading=true"
    - "KPICard renders alert variant with colored background (pink for past target, yellow for trending)"
    - "Table rows highlight on hover with subtle green tint"
    - "CurrencyDisplay formats numbers as USD currency"
    - "TrendIndicator shows up/down arrow with green/red coloring"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Button with variants: primary, secondary, ghost, cta"
      exports: ["Button"]
    - path: "src/components/ui/card.tsx"
      provides: "Card container with THEME.md styling"
      exports: ["Card"]
    - path: "src/components/ui/kpi-card.tsx"
      provides: "KPI card with icon, value, trend, loading, and alert variants"
      exports: ["KPICard"]
    - path: "src/components/ui/badge.tsx"
      provides: "Generic badge component"
      exports: ["Badge"]
    - path: "src/components/ui/table.tsx"
      provides: "Table with subtle dividers, hover rows, mobile card transformation"
      exports: ["Table", "TableHeader", "TableBody", "TableRow", "TableHead", "TableCell"]
    - path: "src/components/ui/input.tsx"
      provides: "Styled input field"
      exports: ["Input"]
    - path: "src/components/ui/skeleton.tsx"
      provides: "Skeleton loading placeholder with pulse animation"
      exports: ["Skeleton"]
    - path: "src/components/ui/status-badge.tsx"
      provides: "Status pill badge with color-coded variants"
      exports: ["StatusBadge"]
    - path: "src/components/ui/trend-indicator.tsx"
      provides: "Trend arrow with percentage"
      exports: ["TrendIndicator"]
    - path: "src/components/ui/currency-display.tsx"
      provides: "USD currency formatter"
      exports: ["CurrencyDisplay"]
    - path: "src/components/ui/index.ts"
      provides: "Barrel export for all UI components"
      min_lines: 10
  key_links:
    - from: "src/components/ui/kpi-card.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "import Skeleton for loading state"
      pattern: "Skeleton"
    - from: "src/components/ui/kpi-card.tsx"
      to: "src/components/ui/trend-indicator.tsx"
      via: "import TrendIndicator for trend display"
      pattern: "TrendIndicator"
    - from: "src/components/ui/index.ts"
      to: "src/components/ui/*.tsx"
      via: "barrel re-exports all components"
      pattern: "export"
---

<objective>
Build all 10 reusable UI components from the design system: Button, Card, KPICard, Badge, Table, Input, Skeleton, StatusBadge, TrendIndicator, CurrencyDisplay. Each component follows THEME.md styling and the patterns established in the research phase.

Purpose: Create the complete component library that Phases 4-7 compose into role-specific views. Getting these right means zero rework later.
Output: 10 themed, typed, exportable UI components with a barrel export file.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffolding-and-design-system/01-CONTEXT.md
@.planning/phases/01-scaffolding-and-design-system/01-RESEARCH.md
@.planning/phases/01-scaffolding-and-design-system/01-01-SUMMARY.md
@THEME.md

<interfaces>
<!-- From Plan 01 — theme tokens and utilities available -->

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/app/globals.css (Tailwind utility classes available):
- Colors: bg-forest, bg-emerald, bg-card, text-text-primary, text-text-secondary, bg-status-ready, bg-status-attention, bg-status-blocked, bg-status-progress, bg-alert-past-target, bg-alert-trending, text-positive, text-negative, bg-chartreuse, text-forest
- Radius: rounded-card (16px), rounded-pill (9999px), rounded-badge (8px)
- Fonts: font-heading (Plus Jakarta Sans), font-body (Geist)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build primitive UI components (Button, Card, Badge, Input, Skeleton, StatusBadge)</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/ui/input.tsx, src/components/ui/skeleton.tsx, src/components/ui/status-badge.tsx</files>
  <action>
Create 6 primitive components. All components use clsx (or cn from utils) for class merging, forwardRef where applicable, and export named exports.

1. **Button** (`button.tsx`):
   - Variants: primary (bg-emerald text-white hover:bg-emerald-dark), secondary (bg-card text-text-primary border border-gray-200 hover:bg-gray-50), ghost (bg-transparent text-text-secondary hover:text-text-primary hover:bg-white/10), cta (bg-chartreuse text-forest font-bold hover:brightness-95)
   - Sizes: sm (px-3 py-1.5 text-sm), md (px-4 py-2 text-base), lg (px-6 py-3 text-lg)
   - Use forwardRef, extend ButtonHTMLAttributes
   - Base classes: inline-flex items-center justify-center font-heading font-semibold transition-colors rounded-pill focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald focus-visible:ring-offset-2 disabled:opacity-50 disabled:pointer-events-none

2. **Card** (`card.tsx`):
   - Simple container: bg-card rounded-card p-6 shadow-sm
   - Accept className for overrides
   - Accept optional `variant` prop: "default" | "flush" (flush = no padding, for table cards)
   - Render as div with children

3. **Badge** (`badge.tsx`):
   - Generic badge/chip: inline-flex items-center text-xs font-medium rounded-badge px-2.5 py-0.5
   - Variants: default (bg-gray-100 text-text-primary), emerald (bg-emerald/10 text-emerald-dark), outline (border border-gray-200 text-text-secondary)
   - Accept className for overrides

4. **Input** (`input.tsx`):
   - Styled input: w-full rounded-badge border border-gray-200 bg-card px-3 py-2 text-sm font-body text-text-primary placeholder:text-text-secondary focus:outline-none focus:ring-2 focus:ring-emerald focus:border-transparent transition-colors disabled:opacity-50
   - Use forwardRef, extend InputHTMLAttributes
   - Optional `label` prop that renders a label element above the input with text-sm font-medium text-text-primary mb-1
   - Optional `error` prop that renders error text below input in text-negative text-sm mt-1

5. **Skeleton** (`skeleton.tsx`):
   - Animated placeholder: animate-pulse bg-gray-200 rounded-badge
   - Accept className to set dimensions (width/height)
   - Set aria-hidden="true" for accessibility
   - Simple div element

6. **StatusBadge** (`status-badge.tsx`):
   - Filled pill style with solid background and text (per user decision)
   - Status type: "completed" | "ready" | "attention" | "blocked" | "in-progress"
   - Color mapping:
     - completed/ready: bg-status-ready text-white
     - attention: bg-status-attention text-white
     - blocked: bg-status-blocked text-text-primary (dark text on yellow per Pitfall 5 — WCAG contrast)
     - in-progress: bg-status-progress text-white
   - Label mapping: completed="Completed", ready="Ready", attention="NEEDS ATTENTION", blocked="Blocked", in-progress="In Progress"
   - Base classes: inline-flex px-3 py-1 text-xs font-semibold rounded-pill
   - Accept optional `label` override prop to display custom text
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 primitive components exist with correct TypeScript types, theme classes, and accessibility attributes. StatusBadge uses dark text on yellow (blocked) status for WCAG contrast. Build passes with no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build data display components (TrendIndicator, CurrencyDisplay, KPICard, Table) and barrel export</name>
  <files>src/components/ui/trend-indicator.tsx, src/components/ui/currency-display.tsx, src/components/ui/kpi-card.tsx, src/components/ui/table.tsx, src/components/ui/index.ts</files>
  <action>
Create 4 data display components and the barrel export file.

1. **TrendIndicator** (`trend-indicator.tsx`):
   - Props: direction ("up" | "down"), percentage (number)
   - Icon: TrendingUp or TrendingDown from lucide-react, size={14}
   - Color: text-positive for up, text-negative for down
   - Layout: flex items-center gap-1 text-sm mt-1
   - Percentage displayed with 1 decimal (toFixed(1)) and "%" suffix
   - Use tabular-nums font-medium on the percentage span

2. **CurrencyDisplay** (`currency-display.tsx`):
   - Use Intl.NumberFormat("en-US", { style: "currency", currency: "USD", minimumFractionDigits: 0, maximumFractionDigits: 0 })
   - Create formatter as module-level constant (not inside component — per research Don't Hand-Roll section)
   - Props: amount (number), className (optional string)
   - Render span with tabular-nums class + className override
   - Named export: CurrencyDisplay

3. **KPICard** (`kpi-card.tsx`):
   - Props interface: icon (LucideIcon type from lucide-react), label (string), value (string | number), trend (optional { direction: "up" | "down"; percentage: number }), variant ("default" | "alert-past" | "alert-trending", default "default"), loading (boolean, default false)
   - Loading state: When loading=true, render a Card with 3 Skeleton elements matching the card's dimensions (icon placeholder w-10 h-10, value placeholder w-24 h-8, label placeholder w-16 h-4) — per user decision that KPICard renders its own Skeleton
   - Normal state:
     - Background: bg-card for default, bg-alert-past-target for alert-past, bg-alert-trending for alert-trending
     - Icon badge: w-10 h-10 bg-forest rounded-badge flex items-center justify-center, icon rendered at size={20} in text-white
     - Label: text-text-secondary text-sm mb-1
     - Value: text-3xl font-heading font-bold tabular-nums
     - Trend: render TrendIndicator if trend prop provided
   - Wrap in rounded-card p-6 shadow-sm
   - Import Skeleton and TrendIndicator from sibling files (relative import)

4. **Table** (`table.tsx`):
   - Compound component pattern with named exports: Table, TableHeader, TableBody, TableRow, TableHead, TableCell
   - Table: w-full border-collapse text-sm
   - TableHeader: wrapper for thead
   - TableHead (th): text-left text-text-secondary font-medium text-xs uppercase tracking-wider py-3 px-4
   - TableBody: wrapper for tbody, children are TableRows
   - TableRow (tr): border-b border-gray-100 hover:bg-emerald/5 transition-colors cursor-pointer (per user decision — clicking row navigates to detail view)
   - TableCell (td): py-3 px-4 text-text-primary
   - Accept optional `onRowClick` on TableRow for navigation
   - Mobile transformation: On screens below md:, the Table wrapper applies a CSS class that transforms the table layout. Use a responsive approach:
     - Wrap table in a div with `overflow-x-auto` for horizontal scroll as the baseline
     - Add a comment noting that per-view mobile card transformations will be handled at the page level in later phases (the Table component provides the desktop version; mobile card list is a separate rendering concern handled by consuming components)

5. **Barrel export** (`index.ts`):
   - Re-export all 10 components from their respective files
   - `export { Button } from "./button"` pattern for each
   - Also export relevant types: ButtonProps (if exported), Status type from status-badge, KPICardProps, etc.
  </action>
  <verify>
    <automated>cd "C:/Users/heinz/OneDrive/Desktop/REPO/UnitFlowSolutions" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 10 components exist and build without errors. KPICard renders skeleton when loading=true and alert backgrounds for alert variants. Table uses compound component pattern with hover rows. CurrencyDisplay formats numbers as USD. Barrel export re-exports all 10 components. Total component count: 10 (Button, Card, Badge, Input, Skeleton, StatusBadge, TrendIndicator, CurrencyDisplay, KPICard, Table).</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- All 10 component files exist in src/components/ui/
- index.ts barrel file exports all 10 components
- No raw hex values in component files (all use theme tokens)
- StatusBadge "blocked" variant uses dark text (not white) on yellow background
</verification>

<success_criteria>
- 10 components created: Button, Card, KPICard, Badge, Table, Input, Skeleton, StatusBadge, TrendIndicator, CurrencyDisplay
- All components use THEME.md tokens via Tailwind utility classes (no hard-coded colors)
- KPICard has working loading state and alert variants
- StatusBadge has correct contrast (dark text on yellow)
- Barrel export provides single import path for all components
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-and-design-system/01-02-SUMMARY.md`
</output>
