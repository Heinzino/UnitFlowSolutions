---
phase: 02-authentication-and-property-scoping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/middleware.ts
  - src/middleware.ts
  - src/app/actions/auth.ts
  - src/lib/normalize-property-name.ts
  - src/lib/types/auth.ts
  - .env.local.example
  - src/app/login/__tests__/login-page.test.tsx
  - src/app/actions/__tests__/auth.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - SCOPE-04

must_haves:
  truths:
    - "Supabase client factories exist for browser, server, and middleware contexts"
    - "Middleware intercepts every non-static request, refreshes tokens, and enforces role-based routing"
    - "Login server action calls signInWithPassword and redirects to role dashboard"
    - "Logout server action calls signOut and redirects to /login"
    - "Property name normalization handles case and whitespace differences"
  artifacts:
    - path: "src/lib/supabase/client.ts"
      provides: "Browser Supabase client factory"
      exports: ["createClient"]
    - path: "src/lib/supabase/server.ts"
      provides: "Server Supabase client factory"
      exports: ["createClient"]
    - path: "src/lib/supabase/middleware.ts"
      provides: "updateSession helper for middleware"
      exports: ["updateSession"]
    - path: "src/middleware.ts"
      provides: "Route protection and token refresh"
      exports: ["middleware", "config"]
    - path: "src/app/actions/auth.ts"
      provides: "Login and logout server actions"
      exports: ["login", "logout"]
    - path: "src/lib/normalize-property-name.ts"
      provides: "Property name normalization"
      exports: ["normalizePropertyName", "propertyMatches"]
    - path: "src/lib/types/auth.ts"
      provides: "Auth type definitions"
      exports: ["UserRole", "ROLE_ROUTES", "AppMetadata"]
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "import updateSession"
      pattern: "import.*updateSession.*from.*supabase/middleware"
    - from: "src/app/actions/auth.ts"
      to: "src/lib/supabase/server.ts"
      via: "import createClient"
      pattern: "import.*createClient.*from.*supabase/server"
    - from: "src/middleware.ts"
      to: "src/lib/types/auth.ts"
      via: "import ROLE_ROUTES"
      pattern: "ROLE_ROUTES"

user_setup:
  - service: supabase
    why: "Authentication backend"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon/public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key (for admin script only, never NEXT_PUBLIC_)"
    dashboard_config:
      - task: "Create Supabase project (if not already done)"
        location: "https://supabase.com/dashboard"
      - task: "Create users via Authentication -> Users and set app_metadata.role and app_metadata.property_ids using SQL Editor or admin API"
        location: "Supabase Dashboard -> Authentication -> Users"
---

<objective>
Install Supabase packages, create client factories (browser, server, middleware), build the middleware for token refresh and role-based route protection, implement login/logout server actions, define auth types, and create property name normalization utility.

Purpose: This is the auth infrastructure layer. Every subsequent plan in this phase depends on these client factories, middleware, and server actions.
Output: Supabase client factories, middleware, auth actions, type definitions, normalization utility, env template, Wave 0 test stubs.
</objective>

<execution_context>
@C:/Users/heinz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/heinz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-and-property-scoping/02-RESEARCH.md

<interfaces>
<!-- Existing codebase interfaces needed by this plan -->

From src/lib/utils.ts (assumed — standard Next.js utility):
```typescript
export function cn(...inputs: ClassValue[]): string;
```

No other existing code is needed — this plan creates new infrastructure from scratch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Supabase packages and create client factories + types</name>
  <files>src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/supabase/middleware.ts, src/lib/types/auth.ts, .env.local.example</files>
  <action>
1. Install packages: `npm install @supabase/supabase-js @supabase/ssr`

2. Create `src/lib/types/auth.ts` with:
   - `type UserRole = 'pm' | 'dm' | 'exec'`
   - `const ROLE_ROUTES: Record<UserRole, string> = { pm: '/property', dm: '/district', exec: '/executive' }`
   - `interface AppMetadata { role: UserRole; property_ids?: string[] }`
   - `const ROLE_LABELS: Record<UserRole, string> = { pm: 'Property Manager', dm: 'District Manager', exec: 'Executive' }`
   - Export all of these

3. Create `src/lib/supabase/server.ts` — async `createClient()` that uses `cookies()` from `next/headers` with `getAll`/`setAll` cookie config. Follow exact pattern from 02-RESEARCH.md Pattern 2. Uses `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` env vars.

4. Create `src/lib/supabase/client.ts` — `createClient()` using `createBrowserClient` from `@supabase/ssr`. Follow exact pattern from 02-RESEARCH.md Pattern 3.

5. Create `src/lib/supabase/middleware.ts` — `updateSession(request: NextRequest)` helper that creates a server client with the request/response cookie dance, calls `getUser()` for token refresh, implements role-based routing logic:
   - Unauthenticated + not on /login -> redirect to /login
   - Authenticated + on /login -> redirect to role dashboard
   - Authenticated + on wrong role route -> silent redirect to own role dashboard
   - Authenticated + on / -> redirect to role dashboard
   - Uses `ROLE_ROUTES` from `src/lib/types/auth.ts`
   - CRITICAL: No code between `createServerClient` and `getUser()` call
   - CRITICAL: Use `getAll`/`setAll` only, never individual get/set/remove
   - CRITICAL: Reassign `supabaseResponse` inside `setAll` callback

6. Create `.env.local.example` with placeholder values:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
   ```

7. Add `.env.local` to `.gitignore` if not already present.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All four Supabase client files compile without errors, types are exported, env template exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create middleware entry point, auth server actions, property normalization, and Wave 0 test stubs</name>
  <files>src/middleware.ts, src/app/actions/auth.ts, src/lib/normalize-property-name.ts, src/lib/__tests__/normalize-property-name.test.ts, src/middleware.test.ts, src/app/login/__tests__/login-page.test.tsx, src/app/actions/__tests__/auth.test.ts</files>
  <behavior>
    - normalizePropertyName("Oak Estates ") returns "oak estates"
    - normalizePropertyName("  PINE RIDGE") returns "pine ridge"
    - propertyMatches("Oak Estates", ["oak estates", "pine ridge"]) returns true
    - propertyMatches("Unknown", ["oak estates"]) returns false
    - propertyMatches("oak estates ", ["Oak Estates"]) returns true (bidirectional normalization)
  </behavior>
  <action>
1. Create `src/lib/normalize-property-name.ts`:
   - `normalizePropertyName(name: string): string` — returns `name.toLowerCase().trim()`
   - `propertyMatches(airtableName: string, assignedNames: string[]): boolean` — normalizes both sides and checks inclusion
   - `filterByProperties<T>(items: T[], getPropertyName: (item: T) => string, assignedNames: string[]): T[]` — generic filter helper for Phase 3

2. Create `src/lib/__tests__/normalize-property-name.test.ts`:
   - Test all behavior cases above
   - Test empty array returns false
   - Test filterByProperties with mock data

3. Create `src/middleware.ts`:
   - Import `updateSession` from `@/lib/supabase/middleware`
   - Export `async function middleware(request)` that calls `updateSession(request)`
   - Export `config` with matcher that excludes static files: `/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)`

4. Create `src/app/actions/auth.ts` with `'use server'` directive:
   - `login(prevState: unknown, formData: FormData)`: calls `createClient()` from server.ts, then `signInWithPassword`, returns `{ error: string }` on failure, `redirect(ROLE_ROUTES[role])` on success. Read role from `data.user.app_metadata.role`. Default to '/property' if role unknown.
   - `logout()`: calls `createClient()`, then `signOut()`, then `redirect('/login')`
   - Import `ROLE_ROUTES` from `@/lib/types/auth`

5. Create `src/middleware.test.ts` (basic structure for future expansion):
   - Test file exists with a placeholder test that confirms the middleware config matcher pattern is exported
   - Note: Full middleware integration tests require mocking @supabase/ssr which is complex; the middleware logic is tested via the updateSession helper pattern

6. Create `src/app/login/__tests__/login-page.test.tsx` — Wave 0 stub test file for AUTH-01:
   - Mock `@supabase/ssr` using `vi.mock('@supabase/ssr')` to stub `createBrowserClient`
   - Mock `react` `useActionState` to return `[null, mockFormAction, false]`
   - Mock `@/app/actions/auth` to stub the `login` action
   - Test stubs (each with `it.todo` or minimal assertion):
     - "renders email and password fields"
     - "renders Sign In button"
     - "displays error message when login fails" (set mock state to `{ error: 'Invalid credentials' }`)
   - These are scaffold tests — Plan 02 will create the actual login page component that makes them pass

7. Create `src/app/actions/__tests__/auth.test.ts` — Wave 0 stub test file for AUTH-02 and AUTH-05:
   - Mock `@supabase/ssr` using `vi.mock('@supabase/ssr')` to stub `createServerClient`
   - Mock `next/headers` cookies
   - Mock `next/navigation` redirect
   - Test stubs (each with `it.todo` or minimal assertion):
     - "login calls signInWithPassword with email and password from formData"
     - "login redirects to role dashboard on success"
     - "login returns error object on failure"
     - "logout calls signOut and redirects to /login"
   - These are scaffold tests that mock the Supabase client and verify the server action calls the correct Supabase methods
  </action>
  <verify>
    <automated>npm test -- --reporter=dot src/lib/__tests__/normalize-property-name.test.ts src/middleware.test.ts src/app/login/__tests__/login-page.test.tsx src/app/actions/__tests__/auth.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Property normalization tests pass, middleware entry point compiles, auth server actions compile with correct signInWithPassword/signOut/redirect calls, Wave 0 test stubs exist for login page (AUTH-01) and auth actions (AUTH-02, AUTH-05)</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (all new files compile)
- `npm test -- --reporter=dot src/lib/__tests__/normalize-property-name` passes
- `npm test -- --reporter=dot src/app/login/__tests__` runs (stubs exist)
- `npm test -- --reporter=dot src/app/actions/__tests__` runs (stubs exist)
- Files exist: `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`, `src/lib/supabase/middleware.ts`, `src/middleware.ts`, `src/app/actions/auth.ts`, `src/lib/types/auth.ts`, `src/lib/normalize-property-name.ts`, `.env.local.example`, `src/app/login/__tests__/login-page.test.tsx`, `src/app/actions/__tests__/auth.test.ts`
</verification>

<success_criteria>
- Supabase packages installed and importable
- Three client factories (browser, server, middleware) follow official @supabase/ssr patterns
- Middleware protects all routes, refreshes tokens, enforces role-based access
- Login action authenticates and redirects by role; logout action signs out and redirects to /login
- Property normalization handles case and whitespace differences with passing tests
- Wave 0 test stubs exist for login page, auth actions, and middleware
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-and-property-scoping/02-01-SUMMARY.md`
</output>
